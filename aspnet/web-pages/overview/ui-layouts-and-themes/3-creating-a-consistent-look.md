---
uid: web-pages/overview/ui-layouts-and-themes/3-creating-a-consistent-look
title: Erstellen eines konsistenten Layouts in ASP.net Web Pages Websites (Razor) | Microsoft-Dokumentation
author: Rick-Anderson
description: Um es effizienter zu machen, Webseiten für Ihre Website zu erstellen, können Sie wiederverwendbare Inhaltsblöcke (z. b. Kopf-und Fußzeilen) für Ihre Website erstellen.
ms.author: riande
ms.date: 03/10/2014
ms.assetid: d7bd001b-6db2-4422-9b78-f3d08b743b00
msc.legacyurl: /web-pages/overview/ui-layouts-and-themes/3-creating-a-consistent-look
msc.type: authoredcontent
ms.openlocfilehash: 3f63ce68ae4c13970ac0df196167ace0b22b592c
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 03/06/2020
ms.locfileid: "78509097"
---
# <a name="creating-a-consistent-layout-in-aspnet-web-pages-razor-sites"></a><span data-ttu-id="889b2-103">Erstellen eines konsistenten Layouts in ASP.net Web Pages Websites (Razor)</span><span class="sxs-lookup"><span data-stu-id="889b2-103">Creating a Consistent Layout in ASP.NET Web Pages (Razor) Sites</span></span>

<span data-ttu-id="889b2-104">von [Tom fitzmacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="889b2-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="889b2-105">In diesem Artikel wird erläutert, wie Sie Layoutseiten in einer ASP.net Web Pages-Website (Razor) verwenden können, um wiederverwendbare Inhaltsblöcke (z. b. Kopf-und Fußzeilen) zu erstellen und ein konsistentes Aussehen für alle Seiten der Website zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="889b2-105">This article explains how you can use layout pages in an ASP.NET Web Pages (Razor) website to create reusable blocks of content (like headers and footers) and to create a consistent look for all the pages in the site.</span></span>
> 
> <span data-ttu-id="889b2-106">**Lernen Sie Folgendes:**</span><span class="sxs-lookup"><span data-stu-id="889b2-106">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="889b2-107">Erstellen wiederverwendbarer Inhaltsblöcke wie Kopf-und Fußzeilen.</span><span class="sxs-lookup"><span data-stu-id="889b2-107">How to create reusable blocks of content like headers and footers.</span></span>
> - <span data-ttu-id="889b2-108">So erstellen Sie ein konsistentes Aussehen für alle Seiten in Ihrer Site mithilfe eines Layouts.</span><span class="sxs-lookup"><span data-stu-id="889b2-108">How to create a consistent look for all the pages in your site using a layout.</span></span>
> - <span data-ttu-id="889b2-109">Übergeben von Daten zur Laufzeit an eine Layoutseite.</span><span class="sxs-lookup"><span data-stu-id="889b2-109">How to pass data at run time to a layout page.</span></span>
> 
> <span data-ttu-id="889b2-110">Dies sind die ASP.NET-Funktionen, die im Artikel eingeführt wurden:</span><span class="sxs-lookup"><span data-stu-id="889b2-110">These are the ASP.NET features introduced in the article:</span></span>
> 
> - <span data-ttu-id="889b2-111">Inhaltsblöcke, bei denen es sich um Dateien handelt, die HTML-formatierte Inhalte enthalten, die auf mehreren Seiten eingefügt werden sollen.</span><span class="sxs-lookup"><span data-stu-id="889b2-111">Content blocks, which are files that contain HTML-formatted content to be inserted in multiple pages.</span></span>
> - <span data-ttu-id="889b2-112">Layoutseiten, bei denen es sich um Seiten mit HTML-formatiertem Inhalt handelt, die von Seiten auf der Website gemeinsam genutzt werden können.</span><span class="sxs-lookup"><span data-stu-id="889b2-112">Layout pages, which are pages that contain HTML-formatted content that can be shared by pages on the website.</span></span>
> - <span data-ttu-id="889b2-113">Die Methoden `RenderPage`, `RenderBody`und `RenderSection`, die angeben ASP.net wo Seitenelemente eingefügt werden sollen.</span><span class="sxs-lookup"><span data-stu-id="889b2-113">The `RenderPage`, `RenderBody`, and `RenderSection` methods, which tell ASP.NET where to insert page elements.</span></span>
> - <span data-ttu-id="889b2-114">Das `PageData` Wörterbuch, mit dem Sie Daten zwischen Inhalts Blöcken und Layoutseiten freigeben können.</span><span class="sxs-lookup"><span data-stu-id="889b2-114">The `PageData` dictionary that lets you share data between content blocks and layout pages.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="889b2-115">Im Tutorial verwendete Software Versionen</span><span class="sxs-lookup"><span data-stu-id="889b2-115">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="889b2-116">ASP.net Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="889b2-116">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="889b2-117">Dieses Tutorial funktioniert auch mit ASP.net Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="889b2-117">This tutorial also works with ASP.NET Web Pages 2.</span></span>

## <a name="about-layout-pages"></a><span data-ttu-id="889b2-118">Informationen zu Layoutseiten</span><span class="sxs-lookup"><span data-stu-id="889b2-118">About Layout Pages</span></span>

<span data-ttu-id="889b2-119">Viele Websites verfügen über Inhalte, die auf jeder Seite angezeigt werden, wie z. b. eine Kopf-und Fußzeile, oder ein Feld, das den Benutzern mitteilt, dass Sie angemeldet sind.</span><span class="sxs-lookup"><span data-stu-id="889b2-119">Many websites have content that's displayed on every page, like a header and footer, or a box that tells users that they're logged in.</span></span> <span data-ttu-id="889b2-120">ASP.NET ermöglicht das Erstellen einer separaten Datei mit einem Inhalts Block, der Text, Markup und Code enthalten kann, genau wie eine reguläre Webseite.</span><span class="sxs-lookup"><span data-stu-id="889b2-120">ASP.NET lets you create a separate file with a content block that can contain text, markup, and code, just like a regular web page.</span></span> <span data-ttu-id="889b2-121">Anschließend können Sie den Inhalts Block in andere Seiten auf der Website einfügen, auf der die Informationen angezeigt werden sollen.</span><span class="sxs-lookup"><span data-stu-id="889b2-121">You can then insert the content block in other pages on the site where you want the information to appear.</span></span> <span data-ttu-id="889b2-122">Auf diese Weise müssen Sie nicht denselben Inhalt kopieren und in jede Seite einfügen.</span><span class="sxs-lookup"><span data-stu-id="889b2-122">That way you don't have to copy and paste the same content into every page.</span></span> <span data-ttu-id="889b2-123">Wenn Sie allgemeine Inhalte wie diese erstellen, ist es auch einfacher, Ihre Website zu aktualisieren.</span><span class="sxs-lookup"><span data-stu-id="889b2-123">Creating common content like this also makes it easier to update your site.</span></span> <span data-ttu-id="889b2-124">Wenn Sie den Inhalt ändern müssen, können Sie nur eine einzelne Datei aktualisieren, und die Änderungen werden dann überall dort widergespiegelt, wo der Inhalt eingefügt wurde.</span><span class="sxs-lookup"><span data-stu-id="889b2-124">If you need to change the content, you can just update a single file, and the changes are then reflected everywhere the content has been inserted.</span></span>

<span data-ttu-id="889b2-125">Das folgende Diagramm zeigt, wie Inhaltsblöcke funktionieren.</span><span class="sxs-lookup"><span data-stu-id="889b2-125">The following diagram shows how content blocks work.</span></span> <span data-ttu-id="889b2-126">Wenn ein Browser eine Seite vom Webserver anfordert, fügt ASP.net die Inhaltsblöcke an dem Punkt ein, an dem die `RenderPage`-Methode auf der Hauptseite aufgerufen wird.</span><span class="sxs-lookup"><span data-stu-id="889b2-126">When a browser requests a page from the web server, ASP.NET inserts the content blocks at the point where the `RenderPage` method is called in the main page.</span></span> <span data-ttu-id="889b2-127">Die fertiggestellte Seite (zusammengeführt) wird dann an den Browser gesendet.</span><span class="sxs-lookup"><span data-stu-id="889b2-127">The finished (merged) page is then sent to the browser.</span></span>

![Konzeptionelles Diagramm, das zeigt, wie die renderpage-Methode eine referenzierte Seite in die aktuelle Seite einfügt.](3-creating-a-consistent-look/_static/image1.jpg)

<span data-ttu-id="889b2-129">In diesem Verfahren erstellen Sie eine Seite, die auf zwei Inhaltsblöcke (eine Kopfzeile und eine Fußzeile) verweist, die sich in separaten Dateien befinden.</span><span class="sxs-lookup"><span data-stu-id="889b2-129">In this procedure, you'll create a page that references two content blocks (a header and a footer) that are located in separate files.</span></span> <span data-ttu-id="889b2-130">Sie können dieselben Inhaltsblöcke auf jeder Seite der Website verwenden.</span><span class="sxs-lookup"><span data-stu-id="889b2-130">You can use these same content blocks in any page in your site.</span></span> <span data-ttu-id="889b2-131">Wenn Sie fertig sind, erhalten Sie eine Seite wie die folgende:</span><span class="sxs-lookup"><span data-stu-id="889b2-131">When you're done, you'll get a page like this:</span></span>

![Screenshot, der eine Seite im Browser anzeigt, die sich aus der Ausführung einer Seite ergibt, die Aufrufe der renderpage-Methode einschließt.](3-creating-a-consistent-look/_static/image2.png)

1. <span data-ttu-id="889b2-133">Erstellen Sie im Stamm Ordner der Website eine Datei namens *Index. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="889b2-133">In the root folder of your website, create a file named *Index.cshtml*.</span></span>
2. <span data-ttu-id="889b2-134">Ersetzen Sie das vorhandene Markup durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-134">Replace the existing markup with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample1.html)]
3. <span data-ttu-id="889b2-135">Erstellen Sie im Stamm Ordner einen Ordner mit dem Namen *Shared*.</span><span class="sxs-lookup"><span data-stu-id="889b2-135">In the root folder, create a folder named *Shared*.</span></span>

    > [!NOTE]
    > <span data-ttu-id="889b2-136">Es ist üblich, Dateien zu speichern, die von Webseiten in einem Ordner mit dem Namen *Shared*gemeinsam genutzt werden.</span><span class="sxs-lookup"><span data-stu-id="889b2-136">It's common practice to store files that are shared among web pages in a folder named *Shared*.</span></span>
4. <span data-ttu-id="889b2-137">Erstellen Sie im frei *gegebenen* Ordner eine Datei mit dem Namen *\_Header. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="889b2-137">In the *Shared* folder, create a file named *\_Header.cshtml*.</span></span>
5. <span data-ttu-id="889b2-138">Ersetzen Sie vorhandene Inhalte durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-138">Replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample2.html)]

    <span data-ttu-id="889b2-139">Beachten Sie, dass der Dateiname *\_Header. cshtml*mit einem Unterstrich (\_) als Präfix ist.</span><span class="sxs-lookup"><span data-stu-id="889b2-139">Notice that the file name is *\_Header.cshtml*, with an underscore (\_) as a prefix.</span></span> <span data-ttu-id="889b2-140">ASP.NET sendet keine Seite an den Browser, wenn der Name mit einem Unterstrich beginnt.</span><span class="sxs-lookup"><span data-stu-id="889b2-140">ASP.NET won't send a page to the browser if its name starts with an underscore.</span></span> <span data-ttu-id="889b2-141">Dies hindert Personen daran, diese Seiten direkt oder anderweitig anzufordern.</span><span class="sxs-lookup"><span data-stu-id="889b2-141">This prevents people from requesting (inadvertently or otherwise) these pages directly.</span></span> <span data-ttu-id="889b2-142">Es ist ratsam, einen Unterstrich zu verwenden, um Seiten zu benennen, die Inhaltsblöcke enthalten, da Benutzer nicht wirklich in der Lage sein sollen, diese Seiten &#8212; , die Sie bereits vorhanden sind, in andere Seiten einzufügen.</span><span class="sxs-lookup"><span data-stu-id="889b2-142">It's a good idea to use an underscore to name pages that have content blocks in them, because you don't really want users to be able to request these pages &#8212; they exist strictly to be inserted into other pages.</span></span>
6. <span data-ttu-id="889b2-143">Erstellen Sie im frei *gegebenen* Ordner eine Datei namens " *\_footer. cshtml* ", und ersetzen Sie den Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-143">In the *Shared* folder, create a file named *\_Footer.cshtml* and replace the content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample3.html)]
7. <span data-ttu-id="889b2-144">Fügen Sie auf der Seite *Index. cshtml* zwei Aufrufe der `RenderPage`-Methode hinzu, wie hier gezeigt:</span><span class="sxs-lookup"><span data-stu-id="889b2-144">In the *Index.cshtml* page, add two calls to the `RenderPage` method, as shown here:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample4.html)]

    <span data-ttu-id="889b2-145">Dies zeigt, wie ein Inhalts Block in eine Webseite eingefügt wird.</span><span class="sxs-lookup"><span data-stu-id="889b2-145">This shows how to insert a content block into a web page.</span></span> <span data-ttu-id="889b2-146">Sie nennen die `RenderPage`-Methode und übergeben ihr den Namen der Datei, deren Inhalt Sie an diesem Punkt einfügen möchten.</span><span class="sxs-lookup"><span data-stu-id="889b2-146">You call the `RenderPage` method and pass it the name of the file whose contents you want to insert at that point.</span></span> <span data-ttu-id="889b2-147">Hier fügen Sie den Inhalt der Dateien " *\_Header. cshtml* " und " *\_footer. cshtml* " in die Datei " *Index. cshtml* " ein.</span><span class="sxs-lookup"><span data-stu-id="889b2-147">Here, you're inserting the contents of the *\_Header.cshtml* and *\_Footer.cshtml* files into the *Index.cshtml* file.</span></span>
8. <span data-ttu-id="889b2-148">Führen Sie die *Index. cshtml* -Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="889b2-148">Run the *Index.cshtml* page in a browser.</span></span> <span data-ttu-id="889b2-149">(Klicken Sie in webmatrix im Arbeitsbereich " **Dateien** " mit der rechten Maustaste auf die Datei, und wählen Sie dann **im Browser starten aus**.)</span><span class="sxs-lookup"><span data-stu-id="889b2-149">(In WebMatrix, in the **Files** workspace, right-click the file and then select **Launch in browser**.)</span></span>
9. <span data-ttu-id="889b2-150">Zeigen Sie im Browser die Seitenquelle an.</span><span class="sxs-lookup"><span data-stu-id="889b2-150">In the browser, view the page source.</span></span> <span data-ttu-id="889b2-151">(Klicken Sie in Internet Explorer z. b. mit der rechten Maustaste auf die Seite, und klicken Sie dann auf **Quelle anzeigen**.)</span><span class="sxs-lookup"><span data-stu-id="889b2-151">(For example, in Internet Explorer, right-click the page and then click **View Source**.)</span></span>

    <span data-ttu-id="889b2-152">Dadurch wird das auf dem Browser gesendete Webseiten Markup angezeigt, das das Markup der Indexseite mit den Inhalts Blöcken kombiniert.</span><span class="sxs-lookup"><span data-stu-id="889b2-152">This lets you see the web page markup that's sent to the browser, which combines the index page markup with the content blocks.</span></span> <span data-ttu-id="889b2-153">Das folgende Beispiel zeigt die Seitenquelle, die für " *Index. cshtml*" gerendert wird.</span><span class="sxs-lookup"><span data-stu-id="889b2-153">The following example shows the page source that's rendered for *Index.cshtml*.</span></span> <span data-ttu-id="889b2-154">Die Aufrufe von `RenderPage`, die Sie in " *Index. cshtml* " eingefügt haben, wurden durch den eigentlichen Inhalt der Header-und footerdateien ersetzt.</span><span class="sxs-lookup"><span data-stu-id="889b2-154">The calls to `RenderPage` that you inserted into *Index.cshtml* have been replaced with the actual contents of the header and footer files.</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample5.html)]

## <a name="creating-a-consistent-look-using-layout-pages"></a><span data-ttu-id="889b2-155">Erstellen eines konsistenten Erscheinungsbild mithilfe von Layoutseiten</span><span class="sxs-lookup"><span data-stu-id="889b2-155">Creating a Consistent Look Using Layout Pages</span></span>

<span data-ttu-id="889b2-156">Bisher haben Sie gesehen, dass es einfach ist, denselben Inhalt auf mehreren Seiten zu integrieren.</span><span class="sxs-lookup"><span data-stu-id="889b2-156">So far you've seen that it's easy to include the same content on multiple pages.</span></span> <span data-ttu-id="889b2-157">Eine strukturiertere Methode zum Erstellen einer konsistenten Suche nach einer Site ist die Verwendung von Layoutseiten.</span><span class="sxs-lookup"><span data-stu-id="889b2-157">A more structured approach to creating a consistent look for a site is to use layout pages.</span></span> <span data-ttu-id="889b2-158">Eine Layoutseite definiert die Struktur einer Webseite, enthält jedoch keinen tatsächlichen Inhalt.</span><span class="sxs-lookup"><span data-stu-id="889b2-158">A layout page defines the structure of a web page, but doesn't contain any actual content.</span></span> <span data-ttu-id="889b2-159">Nachdem Sie eine Layoutseite erstellt haben, können Sie Webseiten erstellen, die den Inhalt enthalten, und Sie dann mit der Layoutseite verknüpfen.</span><span class="sxs-lookup"><span data-stu-id="889b2-159">After you've created a layout page, you can create web pages that contain the content and then link them to the layout page.</span></span> <span data-ttu-id="889b2-160">Wenn diese Seiten angezeigt werden, werden Sie entsprechend der Layoutseite formatiert.</span><span class="sxs-lookup"><span data-stu-id="889b2-160">When these pages are displayed, they'll be formatted according to the layout page.</span></span> <span data-ttu-id="889b2-161">(In diesem Sinne fungiert eine Layoutseite als eine Art Vorlage für Inhalte, die auf anderen Seiten definiert sind.)</span><span class="sxs-lookup"><span data-stu-id="889b2-161">(In this sense, a layout page acts as a kind of template for content that's defined in other pages.)</span></span>

<span data-ttu-id="889b2-162">Die Layoutseite ist genauso wie jede beliebige HTML-Seite, mit dem Unterschied, dass Sie einen aufzurufenden `RenderBody` Methode enthält.</span><span class="sxs-lookup"><span data-stu-id="889b2-162">The layout page is just like any HTML page, except that it contains a call to the `RenderBody` method.</span></span> <span data-ttu-id="889b2-163">Die Position der `RenderBody` Methode auf der Layoutseite bestimmt, wo die Informationen von der Inhaltsseite eingeschlossen werden.</span><span class="sxs-lookup"><span data-stu-id="889b2-163">The position of the `RenderBody` method in the layout page determines where the information from the content page will be included.</span></span>

<span data-ttu-id="889b2-164">Das folgende Diagramm zeigt, wie Inhaltsseiten und Layoutseiten zur Laufzeit kombiniert werden, um die fertige Webseite zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="889b2-164">The following diagram shows how content pages and layout pages are combined at run time to produce the finished web page.</span></span> <span data-ttu-id="889b2-165">Der Browser fordert eine Inhaltsseite an.</span><span class="sxs-lookup"><span data-stu-id="889b2-165">The browser requests a content page.</span></span> <span data-ttu-id="889b2-166">Die Inhaltsseite enthält Code, der die Layoutseite angibt, die für die Struktur der Seite verwendet werden soll.</span><span class="sxs-lookup"><span data-stu-id="889b2-166">The content page has code in it that specifies the layout page to use for the page's structure.</span></span> <span data-ttu-id="889b2-167">Auf der Layoutseite wird der Inhalt an dem Punkt eingefügt, an dem die `RenderBody`-Methode aufgerufen wird.</span><span class="sxs-lookup"><span data-stu-id="889b2-167">In the layout page, the content is inserted at the point where the `RenderBody` method is called.</span></span> <span data-ttu-id="889b2-168">Inhaltsblöcke können auch auf der Layoutseite eingefügt werden, indem Sie die `RenderPage`-Methode wie im vorherigen Abschnitt aufrufen.</span><span class="sxs-lookup"><span data-stu-id="889b2-168">Content blocks can also be inserted into the layout page by calling the `RenderPage` method, the way you did in the previous section.</span></span> <span data-ttu-id="889b2-169">Wenn die Webseite fertig ist, wird Sie an den Browser gesendet.</span><span class="sxs-lookup"><span data-stu-id="889b2-169">When the web page is complete, it's sent to the browser.</span></span>

![Screenshot, der eine Seite im Browser anzeigt, die sich aus der Ausführung einer Seite ergibt, die Aufrufe der RenderBody-Methode einschließt.](3-creating-a-consistent-look/_static/image3.jpg)

<span data-ttu-id="889b2-171">Im folgenden Verfahren wird gezeigt, wie eine Layoutseite erstellt und Inhaltsseiten verknüpft werden.</span><span class="sxs-lookup"><span data-stu-id="889b2-171">The following procedure shows how to create a layout page and link content pages to it.</span></span>

1. <span data-ttu-id="889b2-172">Erstellen Sie im frei *gegebenen* Ordner der Website eine Datei mit dem Namen *\_layout1. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="889b2-172">In the *Shared* folder of your website, create a file named *\_Layout1.cshtml*.</span></span>
2. <span data-ttu-id="889b2-173">Ersetzen Sie vorhandene Inhalte durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-173">Replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample6.html)]

    <span data-ttu-id="889b2-174">Verwenden Sie die `RenderPage`-Methode in einer Layoutseite, um Inhaltsblöcke einzufügen.</span><span class="sxs-lookup"><span data-stu-id="889b2-174">You use the `RenderPage` method in a layout page to insert content blocks.</span></span> <span data-ttu-id="889b2-175">Eine Layoutseite kann nur einen aufzurufenden `RenderBody`-Methode enthalten.</span><span class="sxs-lookup"><span data-stu-id="889b2-175">A layout page can contain only one call to the `RenderBody` method.</span></span>
3. <span data-ttu-id="889b2-176">Erstellen Sie im frei *gegebenen* Ordner eine Datei mit dem Namen *\_Header2. cshtml* , und ersetzen Sie vorhandene Inhalte durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-176">In the *Shared* folder, create a file named *\_Header2.cshtml* and replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample7.html)]
4. <span data-ttu-id="889b2-177">Erstellen Sie im Stamm Ordner einen neuen Ordner, und benennen Sie ihn *Stile*.</span><span class="sxs-lookup"><span data-stu-id="889b2-177">In the root folder, create a new folder and name it *Styles*.</span></span>
5. <span data-ttu-id="889b2-178">Erstellen Sie im Ordner " *Stile* " eine Datei mit dem Namen " *Site. CSS* ", und fügen Sie die folgenden Stildefinitionen hinzu:</span><span class="sxs-lookup"><span data-stu-id="889b2-178">In the *Styles* folder, create a file named *Site.css* and add the following style definitions:</span></span>

    [!code-css[Main](3-creating-a-consistent-look/samples/sample8.css)]

    <span data-ttu-id="889b2-179">Diese Stildefinitionen werden hier nur angezeigt, um zu veranschaulichen, wie Stylesheets mit Layoutseiten verwendet werden können.</span><span class="sxs-lookup"><span data-stu-id="889b2-179">These style definitions are here only to show how style sheets can be used with layout pages.</span></span> <span data-ttu-id="889b2-180">Wenn Sie möchten, können Sie eigene Stile für diese Elemente definieren.</span><span class="sxs-lookup"><span data-stu-id="889b2-180">If you want, you can define your own styles for these elements.</span></span>
6. <span data-ttu-id="889b2-181">Erstellen Sie im Stamm Ordner eine Datei mit dem Namen *Content1. cshtml* , und ersetzen Sie vorhandene Inhalte durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-181">In the root folder, create a file named *Content1.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample9.cshtml)]

    <span data-ttu-id="889b2-182">Dies ist eine Seite, auf der eine Layoutseite verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="889b2-182">This is a page that will use a layout page.</span></span> <span data-ttu-id="889b2-183">Der Codeblock am oberen Rand der Seite gibt an, welche Layoutseite zum Formatieren dieses Inhalts verwendet werden soll.</span><span class="sxs-lookup"><span data-stu-id="889b2-183">The code block at the top of the page indicates which layout page to use to format this content.</span></span>
7. <span data-ttu-id="889b2-184">Führen Sie *Content1. cshtml* in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="889b2-184">Run *Content1.cshtml* in a browser.</span></span> <span data-ttu-id="889b2-185">Die gerenderte Seite verwendet das Format und Stylesheet, das in *\_layout1. cshtml* definiert ist, und den Text (Content), der in *Content1. cshtml*definiert ist.</span><span class="sxs-lookup"><span data-stu-id="889b2-185">The rendered page uses the format and style sheet defined in *\_Layout1.cshtml* and the text (content) defined in *Content1.cshtml*.</span></span>

    ![Klang](3-creating-a-consistent-look/_static/image4.png)

    <span data-ttu-id="889b2-187">Sie können Schritt 6 wiederholen, um zusätzliche Inhaltsseiten zu erstellen, die die gleiche Layoutseite gemeinsam verwenden können.</span><span class="sxs-lookup"><span data-stu-id="889b2-187">You can repeat step 6 to create additional content pages that can then share the same layout page.</span></span>

    > [!NOTE]
    > <span data-ttu-id="889b2-188">Sie können Ihre Website so einrichten, dass Sie automatisch die gleiche Layoutseite für alle Inhaltsseiten in einem Ordner verwenden können.</span><span class="sxs-lookup"><span data-stu-id="889b2-188">You can set up your site so that you can automatically use the same layout page for all the content pages in a folder.</span></span> <span data-ttu-id="889b2-189">Weitere Informationen finden Sie unter [Anpassen des Standort weiten Verhaltens für ASP.net Web Pages](https://go.microsoft.com/fwlink/?LinkId=202906).</span><span class="sxs-lookup"><span data-stu-id="889b2-189">For details, see [Customizing Site-Wide Behavior for ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=202906).</span></span>

## <a name="designing-layout-pages-that-have-multiple-content-sections"></a><span data-ttu-id="889b2-190">Entwerfen von Layoutseiten mit mehreren Inhalts Abschnitten</span><span class="sxs-lookup"><span data-stu-id="889b2-190">Designing Layout Pages That Have Multiple Content Sections</span></span>

<span data-ttu-id="889b2-191">Eine Inhaltsseite kann mehrere Abschnitte enthalten. Dies ist nützlich, wenn Sie Layouts verwenden möchten, die mehrere Bereiche mit austauschbarem Inhalt aufweisen.</span><span class="sxs-lookup"><span data-stu-id="889b2-191">A content page can have multiple sections, which is useful if you want to use layouts that have multiple areas with replaceable content.</span></span> <span data-ttu-id="889b2-192">Auf der Seite Inhalt übergeben Sie jedem Abschnitt einen eindeutigen Namen.</span><span class="sxs-lookup"><span data-stu-id="889b2-192">In the content page, you give each section a unique name.</span></span> <span data-ttu-id="889b2-193">(Der Standardabschnitt ist unbenannt.) Fügen Sie auf der Seite Layout eine `RenderBody` Methode hinzu, um anzugeben, wo der unbenannte (Standard-) Abschnitt angezeigt werden soll.</span><span class="sxs-lookup"><span data-stu-id="889b2-193">(The default section is left unnamed.) In the layout page, you add a `RenderBody` method to specify where the unnamed (default) section should appear.</span></span> <span data-ttu-id="889b2-194">Fügen Sie dann separate `RenderSection` Methoden hinzu, um benannte Abschnitte einzeln zu Rendering.</span><span class="sxs-lookup"><span data-stu-id="889b2-194">You then add separate `RenderSection` methods in order to render named sections individually.</span></span>

<span data-ttu-id="889b2-195">Das folgende Diagramm zeigt, wie ASP.net Inhalte verarbeitet, die in mehrere Abschnitte unterteilt sind.</span><span class="sxs-lookup"><span data-stu-id="889b2-195">The following diagram shows how ASP.NET handles content that's divided into multiple sections.</span></span> <span data-ttu-id="889b2-196">Jeder benannte Abschnitt ist in einem Abschnitts Block auf der Inhaltsseite enthalten.</span><span class="sxs-lookup"><span data-stu-id="889b2-196">Each named section is contained in a section block in the content page.</span></span> <span data-ttu-id="889b2-197">(Sie werden im Beispiel `Header` und `List` benannt.) Das Framework fügt den Inhalts Abschnitt an dem Punkt, an dem die `RenderSection`-Methode aufgerufen wird, in die Layoutseite ein.</span><span class="sxs-lookup"><span data-stu-id="889b2-197">(They're named `Header` and `List` in the example.) The framework inserts content section into the layout page at the point where the `RenderSection` method is called.</span></span> <span data-ttu-id="889b2-198">Der unbenannte (Standard-) Abschnitt wird an dem Punkt eingefügt, an dem die `RenderBody`-Methode aufgerufen wird, wie Sie zuvor gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="889b2-198">The unnamed (default) section is inserted at the point where the `RenderBody` method is called, as you saw earlier.</span></span>

![Konzeptionelles Diagramm, das zeigt, wie die rendersection-Methode Verweis Abschnitte in die aktuelle Seite einfügt.](3-creating-a-consistent-look/_static/image5.jpg)

<span data-ttu-id="889b2-200">In diesem Verfahren wird gezeigt, wie eine Inhaltsseite mit mehreren Inhalts Abschnitten erstellt wird und wie diese mithilfe einer Layoutseite, die mehrere Inhalts Abschnitte unterstützt, erstellt wird.</span><span class="sxs-lookup"><span data-stu-id="889b2-200">This procedure shows how to create a content page that has multiple content sections and how to render it using a layout page that supports multiple content sections.</span></span>

1. <span data-ttu-id="889b2-201">Erstellen Sie im frei *gegebenen* Ordner eine Datei mit dem Namen *\_Layout2. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="889b2-201">In the *Shared* folder, create a file named *\_Layout2.cshtml*.</span></span>
2. <span data-ttu-id="889b2-202">Ersetzen Sie vorhandene Inhalte durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-202">Replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample10.html)]

    <span data-ttu-id="889b2-203">Verwenden Sie die `RenderSection`-Methode, um die Header-und Listen Abschnitte zu Rendering.</span><span class="sxs-lookup"><span data-stu-id="889b2-203">You use the `RenderSection` method to render both the header and list sections.</span></span>
3. <span data-ttu-id="889b2-204">Erstellen Sie im Stamm Ordner eine Datei mit dem Namen *Content2. cshtml* , und ersetzen Sie vorhandene Inhalte durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-204">In the root folder, create a file named *Content2.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample11.cshtml)]

    <span data-ttu-id="889b2-205">Diese Inhaltsseite enthält einen Codeblock am oberen Rand der Seite.</span><span class="sxs-lookup"><span data-stu-id="889b2-205">This content page contains a code block at the top of the page.</span></span> <span data-ttu-id="889b2-206">Jeder benannte Abschnitt ist in einem Abschnitts Block enthalten.</span><span class="sxs-lookup"><span data-stu-id="889b2-206">Each named section is contained in a section block.</span></span> <span data-ttu-id="889b2-207">Der Rest der Seite enthält den Standard Inhalts Abschnitt (unbenannt).</span><span class="sxs-lookup"><span data-stu-id="889b2-207">The rest of the page contains the default (unnamed) content section.</span></span>
4. <span data-ttu-id="889b2-208">Führen Sie *Content2. cshtml* in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="889b2-208">Run *Content2.cshtml* in a browser.</span></span>

    ![Screenshot, der eine Seite im Browser anzeigt, die sich aus der Ausführung einer Seite ergibt, die Aufrufe der rendersection-Methode einschließt.](3-creating-a-consistent-look/_static/image6.png)

## <a name="making-content-sections-optional"></a><span data-ttu-id="889b2-210">Inhalts Abschnitte sind optional</span><span class="sxs-lookup"><span data-stu-id="889b2-210">Making Content Sections Optional</span></span>

<span data-ttu-id="889b2-211">Normalerweise müssen die Abschnitte, die Sie auf einer Inhaltsseite erstellen, den Abschnitten entsprechen, die auf der Layoutseite definiert sind.</span><span class="sxs-lookup"><span data-stu-id="889b2-211">Normally, the sections that you create in a content page have to match sections that are defined in the layout page.</span></span> <span data-ttu-id="889b2-212">Wenn eine der folgenden Aktionen auftritt, können Sie Fehlermeldungen erhalten:</span><span class="sxs-lookup"><span data-stu-id="889b2-212">You can get errors if any of the following occur:</span></span>

- <span data-ttu-id="889b2-213">Die Seite Inhalt enthält einen Abschnitt, der über keinen entsprechenden Abschnitt auf der Layoutseite verfügt.</span><span class="sxs-lookup"><span data-stu-id="889b2-213">The content page contains a section that has no corresponding section in the layout page.</span></span>
- <span data-ttu-id="889b2-214">Die Layoutseite enthält einen Abschnitt, für den kein Inhalt vorhanden ist.</span><span class="sxs-lookup"><span data-stu-id="889b2-214">The layout page contains a section for which there's no content.</span></span>
- <span data-ttu-id="889b2-215">Die Layoutseite enthält Methodenaufrufe, die versuchen, denselben Abschnitt mehrmals zu Rendering auszuführen.</span><span class="sxs-lookup"><span data-stu-id="889b2-215">The layout page includes method calls that try to render the same section more than once.</span></span>

<span data-ttu-id="889b2-216">Sie können dieses Verhalten jedoch für einen benannten Abschnitt überschreiben, indem Sie den Abschnitt So deklarieren, dass er auf der Layoutseite optional ist.</span><span class="sxs-lookup"><span data-stu-id="889b2-216">However, you can override this behavior for a named section by declaring the section to be optional in the layout page.</span></span> <span data-ttu-id="889b2-217">Auf diese Weise können Sie mehrere Inhaltsseiten definieren, die eine Layoutseite gemeinsam verwenden können, die aber möglicherweise keinen Inhalt für einen bestimmten Abschnitt enthalten.</span><span class="sxs-lookup"><span data-stu-id="889b2-217">This lets you define multiple content pages that can share a layout page but that might or might not have content for a specific section.</span></span>

1. <span data-ttu-id="889b2-218">Öffnen Sie *Content2. cshtml* , und entfernen Sie den folgenden Abschnitt:</span><span class="sxs-lookup"><span data-stu-id="889b2-218">Open *Content2.cshtml* and remove the following section:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample12.cshtml)]
2. <span data-ttu-id="889b2-219">Speichern Sie die Seite, und führen Sie Sie dann in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="889b2-219">Save the page and then run it in a browser.</span></span> <span data-ttu-id="889b2-220">Eine Fehlermeldung wird angezeigt, da die Inhaltsseite keinen Inhalt für einen Abschnitt bereitstellt, der auf der Layoutseite definiert ist, nämlich den Header Abschnitt.</span><span class="sxs-lookup"><span data-stu-id="889b2-220">An error message is displayed, because the content page doesn't provide content for a section defined in the layout page, namely the header section.</span></span>

    ![Screenshot, der den Fehler anzeigt, der auftritt, wenn Sie eine Seite ausführen, die die rendersection-Methode aufruft, der entsprechende Abschnitt jedoch nicht bereitgestellt wird.](3-creating-a-consistent-look/_static/image7.png)
3. <span data-ttu-id="889b2-222">Öffnen Sie im frei *gegebenen* Ordner die Seite *\_Layout2. cshtml* , und ersetzen Sie diese Zeile:</span><span class="sxs-lookup"><span data-stu-id="889b2-222">In the *Shared* folder, open the *\_Layout2.cshtml* page and replace this line:</span></span>

    [!code-javascript[Main](3-creating-a-consistent-look/samples/sample13.js)]

    <span data-ttu-id="889b2-223">durch den folgenden Code:</span><span class="sxs-lookup"><span data-stu-id="889b2-223">with the following code:</span></span>

    [!code-javascript[Main](3-creating-a-consistent-look/samples/sample14.js)]

    <span data-ttu-id="889b2-224">Als Alternative können Sie die vorherige Codezeile durch den folgenden Codeblock ersetzen, der die gleichen Ergebnisse erzeugt:</span><span class="sxs-lookup"><span data-stu-id="889b2-224">As an alternative, you could replace the previous line of code with the following code block, which produces the same results:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample15.cshtml)]
4. <span data-ttu-id="889b2-225">Führen Sie die *Content2. cshtml* -Seite in einem Browser erneut aus.</span><span class="sxs-lookup"><span data-stu-id="889b2-225">Run the *Content2.cshtml* page in a browser again.</span></span> <span data-ttu-id="889b2-226">(Wenn diese Seite weiterhin im Browser geöffnet ist, können Sie Sie einfach aktualisieren.) Dieses Mal wird die Seite ohne Fehler angezeigt, auch wenn Sie über keinen Header verfügt.</span><span class="sxs-lookup"><span data-stu-id="889b2-226">(If you still have this page open in the browser, you can just refresh it.) This time the page is displayed with no error, even though it has no header.</span></span>

## <a name="passing-data-to-layout-pages"></a><span data-ttu-id="889b2-227">Übergeben von Daten an Layoutseiten</span><span class="sxs-lookup"><span data-stu-id="889b2-227">Passing Data to Layout Pages</span></span>

<span data-ttu-id="889b2-228">Möglicherweise verfügen Sie über Daten, die auf der Inhaltsseite definiert sind, auf die Sie auf einer Layoutseite verweisen müssen.</span><span class="sxs-lookup"><span data-stu-id="889b2-228">You might have data defined in the content page that you need to refer to in a layout page.</span></span> <span data-ttu-id="889b2-229">Wenn dies der Fall ist, müssen Sie die Daten von der Inhaltsseite an die Layoutseite übergeben.</span><span class="sxs-lookup"><span data-stu-id="889b2-229">If so, you need to pass the data from the content page to the layout page.</span></span> <span data-ttu-id="889b2-230">Beispielsweise möchten Sie möglicherweise den Anmeldestatus eines Benutzers anzeigen, oder Sie können Inhaltsbereiche auf der Grundlage von Benutzereingaben anzeigen oder ausblenden.</span><span class="sxs-lookup"><span data-stu-id="889b2-230">For example, you might want to display the login status of a user, or you might want to show or hide content areas based on user input.</span></span>

<span data-ttu-id="889b2-231">Wenn Sie Daten von einer Inhaltsseite an eine Layoutseite übergeben möchten, können Sie Werte in der `PageData`-Eigenschaft der Inhaltsseite ablegen.</span><span class="sxs-lookup"><span data-stu-id="889b2-231">To pass data from a content page to a layout page, you can put values into the `PageData` property of the content page.</span></span> <span data-ttu-id="889b2-232">Die `PageData`-Eigenschaft ist eine Sammlung von Name-Wert-Paaren, die die Daten enthalten, die Sie zwischen Seiten übergeben möchten.</span><span class="sxs-lookup"><span data-stu-id="889b2-232">The `PageData` property is a collection of name/value pairs that hold the data that you want to pass between pages.</span></span> <span data-ttu-id="889b2-233">Auf der Layoutseite können Sie Werte aus der `PageData`-Eigenschaft lesen.</span><span class="sxs-lookup"><span data-stu-id="889b2-233">In the layout page, you can then read values out of the `PageData` property.</span></span>

<span data-ttu-id="889b2-234">Im folgenden finden Sie ein weiteres Diagramm.</span><span class="sxs-lookup"><span data-stu-id="889b2-234">Here's another diagram.</span></span> <span data-ttu-id="889b2-235">In diesem Beispiel wird gezeigt, wie ASP.net die `PageData`-Eigenschaft verwenden kann, um Werte von einer Inhaltsseite an die Layoutseite zu übergeben.</span><span class="sxs-lookup"><span data-stu-id="889b2-235">This one shows how ASP.NET can use the `PageData` property to pass values from a content page to the layout page.</span></span> <span data-ttu-id="889b2-236">Wenn ASP.net mit dem Erstellen der Webseite beginnt, wird die `PageData`-Sammlung erstellt.</span><span class="sxs-lookup"><span data-stu-id="889b2-236">When ASP.NET begins building the web page, it creates the `PageData` collection.</span></span> <span data-ttu-id="889b2-237">Auf der Seite Inhalt schreiben Sie Code zum Einfügen von Daten in die `PageData` Auflistung.</span><span class="sxs-lookup"><span data-stu-id="889b2-237">In the content page, you write code to put data in the `PageData` collection.</span></span> <span data-ttu-id="889b2-238">Auf Werte in der `PageData` Auflistung kann auch in anderen Abschnitten der Inhaltsseite oder durch zusätzliche Inhaltsblöcke zugegriffen werden.</span><span class="sxs-lookup"><span data-stu-id="889b2-238">Values in the `PageData` collection can also be accessed by other sections in the content page or by additional content blocks.</span></span>

![Konzeptionelles Diagramm, das zeigt, wie eine Inhaltsseite ein PageData-Wörterbuch auffüllen und diese Informationen an die Layoutseite übergeben kann.](3-creating-a-consistent-look/_static/image8.jpg)

<span data-ttu-id="889b2-240">Im folgenden Verfahren wird gezeigt, wie Daten von einer Inhaltsseite an eine Layoutseite übergeben werden.</span><span class="sxs-lookup"><span data-stu-id="889b2-240">The following procedure shows how to pass data from a content page to a layout page.</span></span> <span data-ttu-id="889b2-241">Wenn die Seite ausgeführt wird, wird eine Schaltfläche angezeigt, mit der der Benutzer eine Liste ausblenden oder anzeigen kann, die auf der Layoutseite definiert ist.</span><span class="sxs-lookup"><span data-stu-id="889b2-241">When the page runs, it displays a button that lets the user hide or show a list that's defined in the layout page.</span></span> <span data-ttu-id="889b2-242">Wenn Benutzer auf die Schaltfläche klicken, wird in der `PageData`-Eigenschaft der Wert true/false (Boolean) festgelegt.</span><span class="sxs-lookup"><span data-stu-id="889b2-242">When users click the button, it sets a true/false (Boolean) value in the `PageData` property.</span></span> <span data-ttu-id="889b2-243">Auf der Layoutseite wird dieser Wert gelesen, und wenn der Wert false ist, wird die Liste ausgeblendet.</span><span class="sxs-lookup"><span data-stu-id="889b2-243">The layout page reads that value, and if it's false, hides the list.</span></span> <span data-ttu-id="889b2-244">Der Wert wird auch auf der Seite Inhalt verwendet, um zu bestimmen, ob die Schaltfläche **Liste ausblenden** oder **Liste** anzeigen angezeigt werden soll.</span><span class="sxs-lookup"><span data-stu-id="889b2-244">The value is also used in the content page to determine whether to display the **Hide List** button or the **Show List** button.</span></span>

![Klang](3-creating-a-consistent-look/_static/image9.jpg)

1. <span data-ttu-id="889b2-246">Erstellen Sie im Stamm Ordner eine Datei mit dem Namen *Content3. cshtml* , und ersetzen Sie vorhandene Inhalte durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-246">In the root folder, create a file named *Content3.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample16.cshtml)]

    <span data-ttu-id="889b2-247">Der Code speichert zwei Datenelemente in der `PageData`-Eigenschaft &#8212; mit dem Titel der Webseite und true oder false, um anzugeben, ob eine Liste angezeigt werden soll.</span><span class="sxs-lookup"><span data-stu-id="889b2-247">The code stores two pieces of data in the `PageData` property &#8212; the title of the web page and true or false to specify whether to display a list.</span></span>

    <span data-ttu-id="889b2-248">Beachten Sie, dass Sie mithilfe von ASP.net HTML-Markup mithilfe eines Codeblocks bedingt auf der Seite ablegen können.</span><span class="sxs-lookup"><span data-stu-id="889b2-248">Notice that ASP.NET lets you put HTML markup into the page conditionally using a code block.</span></span> <span data-ttu-id="889b2-249">Beispielsweise bestimmt der `if/else`-Block im Text der Seite, welches Formular angezeigt wird, je nachdem, ob `PageData["ShowList"]` auf true festgelegt ist.</span><span class="sxs-lookup"><span data-stu-id="889b2-249">For example, the `if/else` block in the body of the page determines which form to display depending on whether `PageData["ShowList"]` is set to true.</span></span>
2. <span data-ttu-id="889b2-250">Erstellen Sie im frei *gegebenen* Ordner eine Datei mit dem Namen *\_Layout3. cshtml* , und ersetzen Sie vorhandene Inhalte durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-250">In the *Shared* folder, create a file named *\_Layout3.cshtml* and replace any existing content with the following:</span></span>

    [!code-cshtml[Main](3-creating-a-consistent-look/samples/sample17.cshtml)]

    <span data-ttu-id="889b2-251">Die Layoutseite enthält einen Ausdruck in das `<title>`-Element, das den Titelwert aus der `PageData`-Eigenschaft abruft.</span><span class="sxs-lookup"><span data-stu-id="889b2-251">The layout page includes an expression in the `<title>` element that gets the title value from the `PageData` property.</span></span> <span data-ttu-id="889b2-252">Außerdem wird der `ShowList` Wert der `PageData`-Eigenschaft verwendet, um zu bestimmen, ob der Listen Inhalts Block angezeigt werden soll.</span><span class="sxs-lookup"><span data-stu-id="889b2-252">It also uses the `ShowList` value of the `PageData` property to determine whether to display the list content block.</span></span>
3. <span data-ttu-id="889b2-253">Erstellen Sie im frei *gegebenen* Ordner eine Datei namens *\_List. cshtml* , und ersetzen Sie vorhandene Inhalte durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="889b2-253">In the *Shared* folder, create a file named *\_List.cshtml* and replace any existing content with the following:</span></span>

    [!code-html[Main](3-creating-a-consistent-look/samples/sample18.html)]
4. <span data-ttu-id="889b2-254">Führen Sie die *Content3. cshtml* -Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="889b2-254">Run the *Content3.cshtml* page in a browser.</span></span> <span data-ttu-id="889b2-255">Die Seite wird mit der Liste angezeigt, die auf der linken Seite der Seite angezeigt wird, und der Schaltfläche **Liste ausblenden** unten.</span><span class="sxs-lookup"><span data-stu-id="889b2-255">The page is displayed with the list visible on the left side of the page and a **Hide List** button at the bottom.</span></span>

    ![Screenshot, der die Seite mit der Liste und eine Schaltfläche mit dem Text "Ausblenden der Liste" anzeigt.](3-creating-a-consistent-look/_static/image10.png)
5. <span data-ttu-id="889b2-257">Klicken Sie auf **Liste ausblenden**.</span><span class="sxs-lookup"><span data-stu-id="889b2-257">Click **Hide List**.</span></span> <span data-ttu-id="889b2-258">Die Liste wird nicht mehr angezeigt, und die Schaltfläche wechselt zur **Liste anzeigen**.</span><span class="sxs-lookup"><span data-stu-id="889b2-258">The list disappears and the button changes to **Show List**.</span></span>

    ![Screenshot, der die Seite anzeigt, die die Liste nicht enthält, und eine Schaltfläche mit dem Hinweis "Liste anzeigen".](3-creating-a-consistent-look/_static/image11.png)
6. <span data-ttu-id="889b2-260">Klicken Sie auf die Schaltfläche **Liste anzeigen** , und die Liste wird erneut angezeigt.</span><span class="sxs-lookup"><span data-stu-id="889b2-260">Click the **Show List** button, and the list is displayed again.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="889b2-261">Zusätzliche Ressourcen</span><span class="sxs-lookup"><span data-stu-id="889b2-261">Additional Resources</span></span>

[<span data-ttu-id="889b2-262">Anpassen des Standort weiten Verhaltens für ASP.net Web Pages</span><span class="sxs-lookup"><span data-stu-id="889b2-262">Customizing Site-Wide Behavior for ASP.NET Web Pages</span></span>](https://go.microsoft.com/fwlink/?LinkId=202906)
