---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Anzeigen von Daten in einem Diagramm mit ASP.net Web Pages (Razor) | Microsoft-Dokumentation
author: microsoft
description: In diesem Kapitel wird erläutert, wie Daten in einem Diagramm angezeigt werden. In den vorherigen Kapiteln haben Sie gelernt, wie Daten manuell und in einem Raster angezeigt werden. In diesem Kapitel wird erläutert...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: 6dad67d4e3d38d57a761c567d937d714a3184ea9
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 03/06/2020
ms.locfileid: "78509133"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="2e326-105">Anzeigen von Daten in einem Diagramm mit ASP.net Web Pages (Razor)</span><span class="sxs-lookup"><span data-stu-id="2e326-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="2e326-106">von [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="2e326-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="2e326-107">In diesem Artikel wird erläutert, wie Sie ein Diagramm verwenden, um Daten auf einer ASP.net Web Pages-Website (Razor) mithilfe des `Chart`-Hilfsprogramms anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="2e326-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="2e326-108">**Lernen Sie**Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="2e326-109">Anzeigen von Daten in einem Diagramm.</span><span class="sxs-lookup"><span data-stu-id="2e326-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="2e326-110">Gewusst wie: Formatieren von Diagrammen mithilfe integrierter Designs.</span><span class="sxs-lookup"><span data-stu-id="2e326-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="2e326-111">Das Speichern von Diagrammen und das Zwischenspeichern der Diagramme, um die Leistung zu verbessern.</span><span class="sxs-lookup"><span data-stu-id="2e326-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="2e326-112">Dies sind die ASP.net-Programmierfunktionen, die im Artikel eingeführt wurden:</span><span class="sxs-lookup"><span data-stu-id="2e326-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="2e326-113">Das `Chart`-Hilfsprogramm.</span><span class="sxs-lookup"><span data-stu-id="2e326-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="2e326-114">Die Informationen in diesem Artikel gelten für ASP.net Web Pages 1,0 und Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="2e326-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="2e326-115">Das Diagramm Hilfsprogramm</span><span class="sxs-lookup"><span data-stu-id="2e326-115">The Chart Helper</span></span>

<span data-ttu-id="2e326-116">Wenn Sie Ihre Daten in grafischer Form anzeigen möchten, können Sie `Chart`-Hilfsprogramm verwenden.</span><span class="sxs-lookup"><span data-stu-id="2e326-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="2e326-117">Das `Chart`-Hilfsprogramm kann ein Bild erzeugen, das Daten in einer Vielzahl von Diagrammtypen anzeigt.</span><span class="sxs-lookup"><span data-stu-id="2e326-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="2e326-118">Es unterstützt viele Optionen zum Formatieren und bezeichnen.</span><span class="sxs-lookup"><span data-stu-id="2e326-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="2e326-119">Das `Chart`-Hilfsprogramm kann mehr als 30 Typen von Diagrammen darstellen, einschließlich aller Diagrammtypen, mit denen Sie möglicherweise aus Microsoft Excel oder anderen Tool &#8212; Flächen Diagrammen, Balkendiagrammen, Säulen Diagrammen, Linien Diagrammen und Kreis Diagrammen zusammen mit spezielleren Diagrammen wie Aktien Diagrammen vertraut sind.</span><span class="sxs-lookup"><span data-stu-id="2e326-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="2e326-120">**Flächen Diagramm** ![Beschreibung: Bild des Flächen Diagramm Typs](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="2e326-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="2e326-121">**Balkendiagramm** ![Beschreibung: Bild des Balkendiagramm Typs](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="2e326-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="2e326-122">**Säulendiagramm** ![Beschreibung: Bild des Säulendiagramm Typs](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="2e326-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="2e326-123">**Liniendiagramm** ![Beschreibung: Bild des Liniendiagramm Typs](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="2e326-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="2e326-124">Kreis **Diagramm** ![Beschreibung: Bild des Kreis Diagramm Typs](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="2e326-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="2e326-125">![Beschreibung des **Aktien Diagramms** : Bild des Kursdiagramm Typs](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="2e326-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="2e326-126">Diagrammelemente</span><span class="sxs-lookup"><span data-stu-id="2e326-126">Chart Elements</span></span>

<span data-ttu-id="2e326-127">In Diagrammen werden Daten und zusätzliche Elemente wie Legenden, Achsen, Reihen usw. angezeigt.</span><span class="sxs-lookup"><span data-stu-id="2e326-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="2e326-128">Das folgende Bild zeigt viele der Diagramm Elemente, die Sie anpassen können, wenn Sie das `Chart`-Hilfsprogramm verwenden.</span><span class="sxs-lookup"><span data-stu-id="2e326-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="2e326-129">In diesem Artikel wird gezeigt, wie Sie einige (nicht alle) dieser Elemente festlegen.</span><span class="sxs-lookup"><span data-stu-id="2e326-129">This article shows you how to set some (not all) of these elements.</span></span>

![Beschreibung: Abbildung der Diagramm Elemente](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="2e326-131">Erstellen eines Diagramms aus Daten</span><span class="sxs-lookup"><span data-stu-id="2e326-131">Creating a Chart from Data</span></span>

<span data-ttu-id="2e326-132">Die Daten, die Sie in einem Diagramm anzeigen, können aus einem Array, aus den Ergebnissen, die von einer Datenbank zurückgegeben werden, oder aus Daten in einer XML-Datei bestehen.</span><span class="sxs-lookup"><span data-stu-id="2e326-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="2e326-133">Verwenden eines Arrays</span><span class="sxs-lookup"><span data-stu-id="2e326-133">Using an Array</span></span>

<span data-ttu-id="2e326-134">Wie in [Einführung in ASP.net Web Pages Programmierung mithilfe der Razor-Syntax](https://go.microsoft.com/fwlink/?LinkId=202890)erläutert, können Sie mit einem Array eine Auflistung ähnlicher Elemente in einer einzelnen Variablen speichern.</span><span class="sxs-lookup"><span data-stu-id="2e326-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="2e326-135">Sie können Arrays verwenden, um die Daten zu enthalten, die Sie in das Diagramm aufnehmen möchten.</span><span class="sxs-lookup"><span data-stu-id="2e326-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="2e326-136">In diesem Verfahren wird gezeigt, wie Sie ein Diagramm aus Daten in Arrays mithilfe des Standard Diagramm Typs erstellen können.</span><span class="sxs-lookup"><span data-stu-id="2e326-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="2e326-137">Außerdem wird gezeigt, wie das Diagramm auf der Seite angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="2e326-138">Erstellen Sie eine neue Datei mit dem Namen *chartarraybasic. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2e326-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="2e326-139">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="2e326-140">Der Code erstellt zunächst ein neues Diagramm und legt seine Breite und Höhe fest.</span><span class="sxs-lookup"><span data-stu-id="2e326-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="2e326-141">Sie geben den Diagramm Titel an, indem Sie die `AddTitle`-Methode verwenden.</span><span class="sxs-lookup"><span data-stu-id="2e326-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="2e326-142">Zum Hinzufügen von Daten verwenden Sie die `AddSeries`-Methode.</span><span class="sxs-lookup"><span data-stu-id="2e326-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="2e326-143">In diesem Beispiel verwenden Sie die Parameter `name`, `xValue`und `yValues` der `AddSeries`-Methode.</span><span class="sxs-lookup"><span data-stu-id="2e326-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="2e326-144">Der `name`-Parameter wird in der Diagramm Legende angezeigt.</span><span class="sxs-lookup"><span data-stu-id="2e326-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="2e326-145">Der `xValue`-Parameter enthält ein Array von Daten, das entlang der horizontalen Achse des Diagramms angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="2e326-146">Der `yValues`-Parameter enthält ein Array von Daten, das zum Zeichnen der vertikalen Punkte des Diagramms verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="2e326-147">Die `Write`-Methode rendert tatsächlich das Diagramm.</span><span class="sxs-lookup"><span data-stu-id="2e326-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="2e326-148">Da Sie in diesem Fall keinen Diagrammtyp angegeben haben, rendert das `Chart`-Hilfsprogramm sein Standard Diagramm, bei dem es sich um ein Säulendiagramm handelt.</span><span class="sxs-lookup"><span data-stu-id="2e326-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="2e326-149">Führen Sie die Seite im Browser aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-149">Run the page in the browser.</span></span> <span data-ttu-id="2e326-150">Der Browser zeigt das Diagramm an.</span><span class="sxs-lookup"><span data-stu-id="2e326-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="2e326-151">Verwenden einer Datenbankabfrage für Diagramm Daten</span><span class="sxs-lookup"><span data-stu-id="2e326-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="2e326-152">Wenn sich die zu diagrammenden Informationen in einer Datenbank befinden, können Sie eine Datenbankabfrage ausführen und dann die Daten aus den Ergebnissen verwenden, um das Diagramm zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="2e326-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="2e326-153">In diesem Verfahren wird gezeigt, wie Sie die Daten aus der Datenbank lesen und anzeigen, die Sie im Artikel Einführung in die [Arbeit mit einer Datenbank an ASP.net Web Pages-Standorten](https://go.microsoft.com/fwlink/?LinkId=202893)erstellt haben.</span><span class="sxs-lookup"><span data-stu-id="2e326-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="2e326-154">Fügen Sie dem Stammverzeichnis der Website eine *App\_Daten* Ordner hinzu, wenn der Ordner nicht bereits vorhanden ist.</span><span class="sxs-lookup"><span data-stu-id="2e326-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="2e326-155">Fügen Sie im Ordner *App\_Data* die Datenbankdatei " *smallfolder. sdf* " hinzu, die unter Einführung in das [Arbeiten mit einer Datenbank an ASP.net Web Pages Websites](https://go.microsoft.com/fwlink/?LinkId=202893)beschrieben wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="2e326-156">Erstellen Sie eine neue Datei mit dem Namen *chartdataquery. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2e326-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="2e326-157">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="2e326-158">Der Code öffnet zunächst die Datenbank smallbakery und weist Sie einer Variablen mit dem Namen `db`zu.</span><span class="sxs-lookup"><span data-stu-id="2e326-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="2e326-159">Diese Variable stellt ein `Database` Objekt dar, das verwendet werden kann, um aus der Datenbank zu lesen und in diese zu schreiben.</span><span class="sxs-lookup"><span data-stu-id="2e326-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="2e326-160">Als Nächstes führt der Code eine SQL-Abfrage aus, um den Namen und den Preis der einzelnen Produkte zu erhalten.</span><span class="sxs-lookup"><span data-stu-id="2e326-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="2e326-161">Der Code erstellt ein neues Diagramm und übergibt die Datenbankabfrage durch Aufrufen der `DataBindTable`-Methode des Diagramms.</span><span class="sxs-lookup"><span data-stu-id="2e326-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="2e326-162">Diese Methode nimmt zwei Parameter an: der `dataSource`-Parameter ist für die Daten aus der Abfrage, und mit dem `xField`-Parameter können Sie festlegen, welche Datenspalte für die x-Achse des Diagramms verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="2e326-163">Als Alternative zur Verwendung der `DataBindTable`-Methode können Sie die `AddSeries`-Methode des `Chart`-Hilfsprogramms verwenden.</span><span class="sxs-lookup"><span data-stu-id="2e326-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="2e326-164">Mit der `AddSeries`-Methode können Sie die `xValue`-und `yValues`-Parameter festlegen.</span><span class="sxs-lookup"><span data-stu-id="2e326-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="2e326-165">Anstatt beispielsweise die `DataBindTable`-Methode wie folgt zu verwenden:</span><span class="sxs-lookup"><span data-stu-id="2e326-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="2e326-166">Sie können die `AddSeries`-Methode wie folgt verwenden:</span><span class="sxs-lookup"><span data-stu-id="2e326-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="2e326-167">Beide erzeugen die gleichen Ergebnisse.</span><span class="sxs-lookup"><span data-stu-id="2e326-167">Both render the same results.</span></span> <span data-ttu-id="2e326-168">Die `AddSeries`-Methode ist flexibler, da Sie den Diagrammtyp und die Daten explizit angeben können, aber die `DataBindTable`-Methode ist einfacher zu verwenden, wenn Sie die zusätzliche Flexibilität nicht benötigen.</span><span class="sxs-lookup"><span data-stu-id="2e326-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="2e326-169">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="2e326-170">Verwenden von XML-Daten</span><span class="sxs-lookup"><span data-stu-id="2e326-170">Using XML Data</span></span>

<span data-ttu-id="2e326-171">Die dritte Option zum Diagramm ist die Verwendung einer XML-Datei als Daten für das Diagramm.</span><span class="sxs-lookup"><span data-stu-id="2e326-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="2e326-172">Hierfür ist es erforderlich, dass die XML-Datei auch eine Schema Datei (*XSD* -Datei) enthält, die die XML-Struktur beschreibt.</span><span class="sxs-lookup"><span data-stu-id="2e326-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="2e326-173">In diesem Verfahren wird gezeigt, wie Sie Daten aus einer XML-Datei lesen.</span><span class="sxs-lookup"><span data-stu-id="2e326-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="2e326-174">Erstellen Sie im Ordner *App\_Data* eine neue XML-Datei mit dem Namen " *Data. XML*".</span><span class="sxs-lookup"><span data-stu-id="2e326-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="2e326-175">Ersetzen Sie den vorhandenen XML-Code durch den folgenden Code. Dies sind einige XML-Daten zu Mitarbeitern in einem fiktiven Unternehmen.</span><span class="sxs-lookup"><span data-stu-id="2e326-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="2e326-176">Erstellen Sie im Ordner *App\_Data* eine neue XML-Datei mit dem Namen " *Data. xsd*".</span><span class="sxs-lookup"><span data-stu-id="2e326-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="2e326-177">(Beachten Sie, dass diese Zeit die Erweiterung *. xsd*ist.)</span><span class="sxs-lookup"><span data-stu-id="2e326-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="2e326-178">Ersetzen Sie den vorhandenen XML-Code durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="2e326-179">Erstellen Sie im Stammverzeichnis der Website eine neue Datei mit dem Namen *chartdataxml. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2e326-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="2e326-180">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="2e326-181">Der Code erstellt zunächst ein `DataSet`-Objekt.</span><span class="sxs-lookup"><span data-stu-id="2e326-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="2e326-182">Dieses Objekt wird verwendet, um die aus der XML-Datei gelesenen Daten zu verwalten und entsprechend den Informationen in der Schema Datei zu organisieren.</span><span class="sxs-lookup"><span data-stu-id="2e326-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="2e326-183">(Beachten Sie, dass der Anfang des Codes die-Anweisung `using SystemData`enthält.</span><span class="sxs-lookup"><span data-stu-id="2e326-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="2e326-184">Dies ist erforderlich, um mit dem `DataSet` Objekt arbeiten zu können.</span><span class="sxs-lookup"><span data-stu-id="2e326-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="2e326-185">Weitere Informationen finden Sie weiter unten in diesem Artikel unter [&quot;Verwenden von&quot;-Anweisungen und voll qualifizierten Namen](#SB_UsingStatements) .)</span><span class="sxs-lookup"><span data-stu-id="2e326-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="2e326-186">Als Nächstes erstellt der Code auf Grundlage des Datasets ein `DataView`-Objekt.</span><span class="sxs-lookup"><span data-stu-id="2e326-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="2e326-187">Die Datenansicht stellt ein Objekt bereit, an &#8212; das das Diagramm gebunden werden kann, d. h., es wird gelesen und gezeichnet.</span><span class="sxs-lookup"><span data-stu-id="2e326-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="2e326-188">Das Diagramm bindet die Daten mithilfe der `AddSeries`-Methode, wie Sie zuvor beim darstellen der Array Daten gesehen haben, mit dem Unterschied, dass die Parameter `xValue` und `yValues` auf das `DataView` Objekt festgelegt sind.</span><span class="sxs-lookup"><span data-stu-id="2e326-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="2e326-189">Außerdem wird in diesem Beispiel gezeigt, wie ein bestimmter Diagrammtyp angegeben wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="2e326-190">Wenn die Daten in der `AddSeries`-Methode hinzugefügt werden, wird der `chartType`-Parameter auch zum Anzeigen eines Kreis Diagramms festgelegt.</span><span class="sxs-lookup"><span data-stu-id="2e326-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="2e326-191">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="2e326-192">"Using"-Anweisungen und voll qualifizierte Namen</span><span class="sxs-lookup"><span data-stu-id="2e326-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="2e326-193">Die .NET Framework, die ASP.net Web Pages mit Razor-Syntax auf basiert, besteht aus vielen Tausenden von Komponenten (Klassen).</span><span class="sxs-lookup"><span data-stu-id="2e326-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="2e326-194">Um die Arbeit mit all diesen Klassen zu gewährleisten, sind Sie in *Namespaces*organisiert, die in gewisser Weise Bibliotheken ähneln.</span><span class="sxs-lookup"><span data-stu-id="2e326-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="2e326-195">Der `System.Web`-Namespace enthält z. b. Klassen, die Browser-/Serverkommunikation unterstützen, der `System.Xml`-Namespace enthält Klassen, die zum Erstellen und Lesen von XML-Dateien verwendet werden, und der `System.Data`-Namespace enthält Klassen, mit denen Sie mit Daten arbeiten können.</span><span class="sxs-lookup"><span data-stu-id="2e326-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="2e326-196">Um auf eine bestimmte Klasse im .NET Framework zuzugreifen, muss Code nicht nur den Klassennamen, sondern auch den Namespace kennen, in dem sich die Klasse befindet.</span><span class="sxs-lookup"><span data-stu-id="2e326-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="2e326-197">Um z. b. das `Chart`-Hilfsprogramm zu verwenden, muss der Code die `System.Web.Helpers.Chart` Klasse finden, die den Namespace (`System.Web.Helpers`) mit dem Klassennamen (`Chart`) kombiniert.</span><span class="sxs-lookup"><span data-stu-id="2e326-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="2e326-198">Dies wird als *voll qualifizierter* Name &#8212; der Klasse bezeichnet und ist ein eindeutiger Speicherort, der sich innerhalb der vastness des .NET Framework befindet.</span><span class="sxs-lookup"><span data-stu-id="2e326-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="2e326-199">Im Code sieht dies wie folgt aus:</span><span class="sxs-lookup"><span data-stu-id="2e326-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="2e326-200">Allerdings ist es mühsam (und fehleranfällig), diese langen, voll qualifizierten Namen jedes Mal zu verwenden, wenn Sie auf eine Klasse oder ein Hilfsprogramm verweisen möchten.</span><span class="sxs-lookup"><span data-stu-id="2e326-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="2e326-201">Um die Verwendung von Klassennamen zu vereinfachen, können Sie daher die Namespaces *importieren* , an denen Sie interessiert sind. Dies ist in der Regel nur eine Handvoll von den vielen Namespaces im .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="2e326-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="2e326-202">Wenn Sie einen Namespace importiert haben, können Sie anstelle des voll qualifizierten Namens (`System.Web.Helpers.Chart`) nur einen Klassennamen (`Chart`) verwenden.</span><span class="sxs-lookup"><span data-stu-id="2e326-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="2e326-203">Wenn der Code ausgeführt wird und auf einen Klassennamen stößt, kann er nur die Namespaces suchen, die Sie importiert haben, um diese Klasse zu finden.</span><span class="sxs-lookup"><span data-stu-id="2e326-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="2e326-204">Wenn Sie ASP.net Web Pages mit Razor-Syntax verwenden, um Webseiten zu erstellen, verwenden Sie in der Regel jedes Mal denselben Satz von Klassen, einschließlich der `WebPage`-Klasse, der verschiedenen Hilfsprogramme usw.</span><span class="sxs-lookup"><span data-stu-id="2e326-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="2e326-205">Um Ihnen die Arbeit beim Importieren der relevanten Namespaces jedes Mal zu ersparen, wenn Sie eine Website erstellen, wird ASP.net so konfiguriert, dass automatisch ein Satz von Kernnamespaces für jede Website importiert wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="2e326-206">Aus diesem Grund mussten Sie sich nicht mit Namespaces befassen oder bis jetzt importieren. alle Klassen, mit denen Sie gearbeitet haben, befinden sich in Namespaces, die bereits für Sie importiert wurden.</span><span class="sxs-lookup"><span data-stu-id="2e326-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="2e326-207">Manchmal müssen Sie jedoch mit einer Klasse arbeiten, die sich nicht in einem Namespace befindet, der automatisch für Sie importiert wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="2e326-208">In diesem Fall können Sie entweder den voll qualifizierten Namen dieser Klasse verwenden, oder Sie können den Namespace, der die Klasse enthält, manuell importieren.</span><span class="sxs-lookup"><span data-stu-id="2e326-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="2e326-209">Um einen Namespace zu importieren, verwenden Sie die `using`-Anweisung (`import` in Visual Basic), wie Sie in einem Beispiel weiter oben in diesem Artikel gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="2e326-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="2e326-210">Beispielsweise befindet sich die `DataSet`-Klasse im `System.Data`-Namespace.</span><span class="sxs-lookup"><span data-stu-id="2e326-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="2e326-211">Der `System.Data`-Namespace ist für ASP.net-Razor-Seiten nicht automatisch verfügbar.</span><span class="sxs-lookup"><span data-stu-id="2e326-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="2e326-212">Damit Sie mit der `DataSet`-Klasse unter Verwendung des voll qualifizierten Namens arbeiten können, können Sie Code wie den folgenden verwenden:</span><span class="sxs-lookup"><span data-stu-id="2e326-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="2e326-213">Wenn Sie die `DataSet`-Klasse wiederholt verwenden müssen, können Sie einen Namespace wie diesen importieren und dann nur den Klassennamen im Code verwenden:</span><span class="sxs-lookup"><span data-stu-id="2e326-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="2e326-214">Sie können `using`-Anweisungen für alle anderen .NET Framework Namespaces hinzufügen, auf die Sie verweisen möchten.</span><span class="sxs-lookup"><span data-stu-id="2e326-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="2e326-215">Wie bereits erwähnt, müssen Sie dies jedoch nicht oft tun, da sich die meisten Klassen, mit denen Sie arbeiten, in Namespaces befinden, die automatisch von ASP.net für die Verwendung auf *cshtml* -und *vbhtml* -Seiten importiert werden.</span><span class="sxs-lookup"><span data-stu-id="2e326-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="2e326-216">Anzeigen von Diagrammen in einer Webseite</span><span class="sxs-lookup"><span data-stu-id="2e326-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="2e326-217">In den Beispielen, die Sie bisher gesehen haben, erstellen Sie ein Diagramm, und das Diagramm wird als Grafik direkt in den Browser gerendert.</span><span class="sxs-lookup"><span data-stu-id="2e326-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="2e326-218">In vielen Fällen möchten Sie jedoch ein Diagramm als Teil einer Seite anzeigen, nicht nur im Browser.</span><span class="sxs-lookup"><span data-stu-id="2e326-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="2e326-219">Hierfür ist ein zweistufiger Prozess erforderlich.</span><span class="sxs-lookup"><span data-stu-id="2e326-219">To do that requires a two-step process.</span></span> <span data-ttu-id="2e326-220">Der erste Schritt besteht darin, eine Seite zu erstellen, die das Diagramm generiert, wie Sie dies bereits gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="2e326-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="2e326-221">Der zweite Schritt besteht darin, das resultierende Bild auf einer anderen Seite anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="2e326-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="2e326-222">Zum Anzeigen des Bilds verwenden Sie ein HTML-`<img>`-Element auf die gleiche Weise wie jedes beliebige Bild.</span><span class="sxs-lookup"><span data-stu-id="2e326-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="2e326-223">Anstatt jedoch auf eine *JPG* -oder *PNG* -Datei zu verweisen, verweist das `<img>`-Element auf die *cshtml* -Datei, die das `Chart` Hilfsprogramm enthält, das das Diagramm erstellt.</span><span class="sxs-lookup"><span data-stu-id="2e326-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="2e326-224">Wenn die Anzeige Seite ausgeführt wird, ruft das `<img>`-Element die Ausgabe des `Chart` Hilfsobjekts ab und rendert das Diagramm.</span><span class="sxs-lookup"><span data-stu-id="2e326-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="2e326-225">Erstellen Sie eine Datei mit dem Namen " *showChart. cshtml*".</span><span class="sxs-lookup"><span data-stu-id="2e326-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="2e326-226">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="2e326-227">Der Code verwendet das `<img>`-Element, um das Diagramm anzuzeigen, das Sie zuvor in der Datei " *chartarraybasic. cshtml* " erstellt haben.</span><span class="sxs-lookup"><span data-stu-id="2e326-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="2e326-228">Führen Sie die Webseite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-228">Run the web page in a browser.</span></span> <span data-ttu-id="2e326-229">Die Datei *showChart. cshtml* zeigt das Diagramm Bild basierend auf dem Code in der Datei " *chartarraybasic. cshtml* " an.</span><span class="sxs-lookup"><span data-stu-id="2e326-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="2e326-230">Formatieren eines Diagramms</span><span class="sxs-lookup"><span data-stu-id="2e326-230">Styling a Chart</span></span>

<span data-ttu-id="2e326-231">Das `Chart`-Hilfsprogramm unterstützt eine große Anzahl von Optionen, mit denen Sie die Darstellung des Diagramms anpassen können.</span><span class="sxs-lookup"><span data-stu-id="2e326-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="2e326-232">Sie können Farben, Schriftarten, Rahmen usw. festlegen.</span><span class="sxs-lookup"><span data-stu-id="2e326-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="2e326-233">Eine einfache Möglichkeit, die Darstellung eines Diagramms anzupassen, ist die Verwendung eines *Designs.*</span><span class="sxs-lookup"><span data-stu-id="2e326-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="2e326-234">Designs sind Informationssammlungen, die angeben, wie ein Diagramm mithilfe von Schriftarten, Farben, Bezeichnungen, Paletten, Rahmen und Effekten dargestellt werden soll.</span><span class="sxs-lookup"><span data-stu-id="2e326-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="2e326-235">(Beachten Sie, dass der Stil eines Diagramms nicht den Diagrammtyp angibt.)</span><span class="sxs-lookup"><span data-stu-id="2e326-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="2e326-236">In der folgenden Tabelle sind die integrierten Themen aufgeführt.</span><span class="sxs-lookup"><span data-stu-id="2e326-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="2e326-237">Design</span><span class="sxs-lookup"><span data-stu-id="2e326-237">Theme</span></span> | <span data-ttu-id="2e326-238">Beschreibung</span><span class="sxs-lookup"><span data-stu-id="2e326-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="2e326-239">Zeigt rote Spalten in einem weißen Hintergrund an.</span><span class="sxs-lookup"><span data-stu-id="2e326-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="2e326-240">Zeigt blaue Spalten in einem blauen Farbverlaufs Hintergrund an.</span><span class="sxs-lookup"><span data-stu-id="2e326-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="2e326-241">Zeigt blaue Spalten in einem grünen Farbverlaufs Hintergrund an.</span><span class="sxs-lookup"><span data-stu-id="2e326-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="2e326-242">Zeigt orangefarbene Spalten in einem gelben Farbverlaufs Hintergrund an.</span><span class="sxs-lookup"><span data-stu-id="2e326-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="2e326-243">Zeigt 3D-rote Spalten in einem weißen Hintergrund an.</span><span class="sxs-lookup"><span data-stu-id="2e326-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="2e326-244">Sie können das Design angeben, das beim Erstellen eines neuen Diagramms verwendet werden soll.</span><span class="sxs-lookup"><span data-stu-id="2e326-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="2e326-245">Erstellen Sie eine neue Datei mit dem Namen *chartstylegreen. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2e326-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="2e326-246">Ersetzen Sie den vorhandenen Inhalt auf der Seite durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="2e326-247">Dieser Code ist identisch mit dem vorherigen Beispiel, in dem die-Datenbank für Daten verwendet wird, beim Erstellen des `Chart`-Objekts wird jedoch der `theme`-Parameter hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="2e326-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="2e326-248">Das folgende Beispiel zeigt den geänderten Code:</span><span class="sxs-lookup"><span data-stu-id="2e326-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="2e326-249">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-249">Run the page in a browser.</span></span> <span data-ttu-id="2e326-250">Sie sehen die gleichen Daten wie zuvor, aber das Diagramm sieht genauer aus:</span><span class="sxs-lookup"><span data-stu-id="2e326-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="2e326-251">Speichern eines Diagramms</span><span class="sxs-lookup"><span data-stu-id="2e326-251">Saving a Chart</span></span>

<span data-ttu-id="2e326-252">Wenn Sie das `Chart`-Hilfsprogramm wie bisher in diesem Artikel gesehen haben, erstellt das-Hilfsprogramm jedes Mal, wenn es aufgerufen wird, das Diagramm von Grund auf neu.</span><span class="sxs-lookup"><span data-stu-id="2e326-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="2e326-253">Falls erforderlich, fragt der Code für das Diagramm auch die Datenbank erneut ab oder liest die XML-Datei erneut, um die Daten zu erhalten.</span><span class="sxs-lookup"><span data-stu-id="2e326-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="2e326-254">In einigen Fällen kann dies ein komplexer Vorgang sein, z. b. wenn die Datenbank, die Sie Abfragen, groß ist, oder wenn die XML-Datei viele Daten enthält.</span><span class="sxs-lookup"><span data-stu-id="2e326-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="2e326-255">Auch wenn das Diagramm nicht viele Daten umfasst, nimmt der Prozess der dynamischen Erstellung eines Images Server Ressourcen an, und wenn viele Personen die Seite oder Seiten anfordern, die das Diagramm anzeigen, kann sich dies auf die Leistung Ihrer Website auswirken.</span><span class="sxs-lookup"><span data-stu-id="2e326-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="2e326-256">Um Ihnen zu helfen, die potenziellen Auswirkungen der Erstellung eines Diagramms auf die Leistung zu reduzieren, können Sie ein Diagramm erstellen, wenn Sie es erstmalig benötigen, und dann speichern.</span><span class="sxs-lookup"><span data-stu-id="2e326-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="2e326-257">Wenn das Diagramm erneut benötigt wird, anstatt es neu zu generieren, können Sie einfach die gespeicherte Version abrufen und diese wiederherstellen.</span><span class="sxs-lookup"><span data-stu-id="2e326-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="2e326-258">Sie können ein Diagramm auf folgende Weise speichern:</span><span class="sxs-lookup"><span data-stu-id="2e326-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="2e326-259">Zwischenspeichern des Diagramms im Arbeitsspeicher des Computers (auf dem Server).</span><span class="sxs-lookup"><span data-stu-id="2e326-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="2e326-260">Speichern Sie das Diagramm als Bilddatei.</span><span class="sxs-lookup"><span data-stu-id="2e326-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="2e326-261">Speichern Sie das Diagramm als XML-Datei.</span><span class="sxs-lookup"><span data-stu-id="2e326-261">Save the chart as an XML file.</span></span> <span data-ttu-id="2e326-262">Mit dieser Option können Sie das Diagramm ändern, bevor Sie es speichern.</span><span class="sxs-lookup"><span data-stu-id="2e326-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="2e326-263">Zwischenspeichern eines Diagramms</span><span class="sxs-lookup"><span data-stu-id="2e326-263">Caching a Chart</span></span>

<span data-ttu-id="2e326-264">Nachdem Sie ein Diagramm erstellt haben, können Sie es Zwischenspeichern.</span><span class="sxs-lookup"><span data-stu-id="2e326-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="2e326-265">Das Zwischenspeichern eines Diagramms bedeutet, dass es nicht neu erstellt werden muss, wenn es erneut angezeigt werden muss.</span><span class="sxs-lookup"><span data-stu-id="2e326-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="2e326-266">Wenn Sie ein Diagramm im Cache speichern, geben Sie einen Schlüssel an, der für dieses Diagramm eindeutig sein muss.</span><span class="sxs-lookup"><span data-stu-id="2e326-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="2e326-267">Im Cache gespeicherte Diagramme können entfernt werden, wenn auf dem Server wenig Arbeitsspeicher verfügbar ist.</span><span class="sxs-lookup"><span data-stu-id="2e326-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="2e326-268">Außerdem wird der Cache gelöscht, wenn die Anwendung aus irgendeinem Grund neu gestartet wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="2e326-269">Daher ist die Standardmethode zum Arbeiten mit einem zwischengespeicherten Diagramm, immer zuerst zu überprüfen, ob es im Cache verfügbar ist, und andernfalls, um das Diagramm zu erstellen oder neu zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="2e326-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="2e326-270">Erstellen Sie im Stammverzeichnis Ihrer Website eine Datei mit dem Namen *showcachedchart. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2e326-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="2e326-271">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="2e326-272">Das `<img>`-Tag enthält ein `src` Attribut, das auf die Datei " *chartsavetocache. cshtml* " verweist und einen Schlüssel als Abfrage Zeichenfolge an die Seite übergibt.</span><span class="sxs-lookup"><span data-stu-id="2e326-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="2e326-273">Der Schlüssel enthält den Wert &quot;mychartkey&quot;.</span><span class="sxs-lookup"><span data-stu-id="2e326-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="2e326-274">Die Datei *chartsavetocache. cshtml* enthält das `Chart` Hilfsprogramm, das das Diagramm erstellt.</span><span class="sxs-lookup"><span data-stu-id="2e326-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="2e326-275">Diese Seite wird in einem Moment erstellt.</span><span class="sxs-lookup"><span data-stu-id="2e326-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="2e326-276">Am Ende der Seite befindet sich ein Link zu einer Seite mit dem Namen *ClearCache. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2e326-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="2e326-277">Das ist eine Seite, die Sie auch in Kürze erstellen werden.</span><span class="sxs-lookup"><span data-stu-id="2e326-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="2e326-278">Sie benötigen " *ClearCache. cshtml* " nur, um das Zwischenspeichern für dieses Beispiel zu testen – es handelt sich nicht um einen Link oder eine Seite, den Sie normalerweise beim Arbeiten mit zwischengespeicherten Diagrammen einschließen.</span><span class="sxs-lookup"><span data-stu-id="2e326-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="2e326-279">Erstellen Sie im Stammverzeichnis Ihrer Website eine neue Datei mit dem Namen *chartsavetocache. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2e326-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="2e326-280">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="2e326-281">Der Code überprüft zunächst, ob etwas als Schlüsselwert in der Abfrage Zeichenfolge übermittelt wurde.</span><span class="sxs-lookup"><span data-stu-id="2e326-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="2e326-282">Wenn dies der Fall ist, versucht der Code, ein Diagramm aus dem Cache zu lesen, indem Sie die `GetFromCache`-Methode aufrufen und den Schlüssel übergibt.</span><span class="sxs-lookup"><span data-stu-id="2e326-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="2e326-283">Wenn sich herausstellt, dass es im Cache unter diesem Schlüssel nichts gibt (was bei der ersten Anforderung des Diagramms der Fall ist), erstellt der Code das Diagramm wie üblich.</span><span class="sxs-lookup"><span data-stu-id="2e326-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="2e326-284">Wenn das Diagramm abgeschlossen ist, speichert der Code es im Cache, indem `SaveToCache`aufgerufen wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="2e326-285">Diese Methode erfordert einen Schlüssel (damit das Diagramm später angefordert werden kann) und die Zeitspanne, in der das Diagramm im Cache gespeichert werden soll.</span><span class="sxs-lookup"><span data-stu-id="2e326-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="2e326-286">(Die genaue Zeit, in der Sie ein Diagramm Zwischenspeichern, hängt davon ab, wie oft die von ihm dargestellten Daten sich ändern können.) Die `SaveToCache`-Methode erfordert auch einen `slidingExpiration` &#8212; -Parameter, wenn dieser auf true festgelegt ist, wird der Timeout-Wert jedes Mal zurückgesetzt, wenn auf das Diagramm zugegriffen wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="2e326-287">In diesem Fall bedeutet dies, dass der Cache Eintrag des Diagramms 2 Minuten nach dem letzten Zugriff auf das Diagramm abläuft.</span><span class="sxs-lookup"><span data-stu-id="2e326-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="2e326-288">(Die Alternative zum gleitenden Ablauf ist die absolute Ablaufzeit, d. h., der Cache Eintrag läuft genau 2 Minuten ab, nachdem er in den Cache eingefügt wurde, unabhängig davon, wie oft auf ihn zugegriffen wurde.)</span><span class="sxs-lookup"><span data-stu-id="2e326-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="2e326-289">Schließlich verwendet der Code die `WriteFromCache`-Methode zum Abrufen und Rendering des Diagramms aus dem Cache.</span><span class="sxs-lookup"><span data-stu-id="2e326-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="2e326-290">Beachten Sie, dass sich diese Methode außerhalb des `if` Blocks befindet, der den Cache überprüft, da Sie das Diagramm aus dem Cache abfängt, um zu beginnen, oder dass Sie im Cache generiert und gespeichert werden musste.</span><span class="sxs-lookup"><span data-stu-id="2e326-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="2e326-291">Beachten Sie, dass die `AddTitle`-Methode im Beispiel einen Zeitstempel enthält.</span><span class="sxs-lookup"><span data-stu-id="2e326-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="2e326-292">(Das aktuelle Datum und die Uhrzeit &#8212; `DateTime.Now` &#8212; dem Titel hinzugefügt.)</span><span class="sxs-lookup"><span data-stu-id="2e326-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="2e326-293">Erstellen Sie eine neue Seite mit dem Namen *ClearCache. cshtml* , und ersetzen Sie deren Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="2e326-294">Diese Seite verwendet das `WebCache`-Hilfsprogramm, um das Diagramm zu entfernen, das in " *chartsavetocache. cshtml*" zwischengespeichert wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="2e326-295">Wie bereits erwähnt, benötigen Sie normalerweise keine Seite wie diese.</span><span class="sxs-lookup"><span data-stu-id="2e326-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="2e326-296">Sie erstellen es nur hier, um das Zwischenspeichern zu vereinfachen.</span><span class="sxs-lookup"><span data-stu-id="2e326-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="2e326-297">Führen Sie die " *showcachedchart. cshtml* "-Webseite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="2e326-298">Die Seite zeigt das Diagramm Bild basierend auf dem Code an, der in der Datei " *chartsavetocache. cshtml* " enthalten ist.</span><span class="sxs-lookup"><span data-stu-id="2e326-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="2e326-299">Notieren Sie sich den Zeitstempel im Diagramm Titel.</span><span class="sxs-lookup"><span data-stu-id="2e326-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Beschreibung: Bild des Basis Diagramms mit Zeitstempel im Diagramm Titel](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="2e326-301">Schließen Sie den Browser.</span><span class="sxs-lookup"><span data-stu-id="2e326-301">Close the browser.</span></span>
8. <span data-ttu-id="2e326-302">Führen Sie die *showcachedchart. cshtml* erneut aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="2e326-303">Beachten Sie, dass der Zeitstempel mit dem Wert vor identisch ist, was bedeutet, dass das Diagramm nicht erneut generiert, sondern stattdessen aus dem Cache gelesen wurde.</span><span class="sxs-lookup"><span data-stu-id="2e326-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="2e326-304">Klicken Sie in *showcachedchart. cshtml*auf den Link **Cache löschen** .</span><span class="sxs-lookup"><span data-stu-id="2e326-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="2e326-305">Dadurch gelangen Sie zu *ClearCache. cshtml*, das meldet, dass der Cache gelöscht wurde.</span><span class="sxs-lookup"><span data-stu-id="2e326-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="2e326-306">Klicken Sie auf den Link **zu showcachedchart. cshtml zurückkehren** , oder führen Sie *showcachedchart. cshtml* in webmatrix erneut aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="2e326-307">Beachten Sie, dass sich der Zeitstempel geändert hat, da der Cache gelöscht wurde.</span><span class="sxs-lookup"><span data-stu-id="2e326-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="2e326-308">Daher musste der Code das Diagramm neu generieren und wieder in den Cache einfügen.</span><span class="sxs-lookup"><span data-stu-id="2e326-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="2e326-309">Speichern eines Diagramms als Bilddatei</span><span class="sxs-lookup"><span data-stu-id="2e326-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="2e326-310">Sie können ein Diagramm auch als Bilddatei (z *. b. als jpg* -Datei) auf dem Server speichern.</span><span class="sxs-lookup"><span data-stu-id="2e326-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="2e326-311">Anschließend können Sie die Bilddatei wie jedes beliebige Bild verwenden.</span><span class="sxs-lookup"><span data-stu-id="2e326-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="2e326-312">Der Vorteil ist, dass die Datei gespeichert und nicht in einem temporären Cache gespeichert wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="2e326-313">Sie können ein neues Diagramm Bild zu unterschiedlichen Zeitpunkten speichern (z. b. stündlich) und dann einen permanenten Datensatz der Änderungen aufbewahren, die im Laufe der Zeit auftreten.</span><span class="sxs-lookup"><span data-stu-id="2e326-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="2e326-314">Beachten Sie, dass Sie sicherstellen müssen, dass Ihre Webanwendung über die Berechtigung zum Speichern einer Datei in dem Ordner auf dem Server verfügt, auf dem Sie die Bilddatei ablegen möchten.</span><span class="sxs-lookup"><span data-stu-id="2e326-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="2e326-315">Erstellen Sie im Stammverzeichnis Ihrer Website einen Ordner mit dem Namen *\_chartfiles* , wenn er nicht bereits vorhanden ist.</span><span class="sxs-lookup"><span data-stu-id="2e326-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="2e326-316">Erstellen Sie im Stammverzeichnis Ihrer Website eine neue Datei mit dem Namen *chartsave. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2e326-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="2e326-317">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="2e326-318">Der Code prüft zunächst, ob die *JPG* -Datei vorhanden ist, indem Sie die `File.Exists`-Methode aufrufen.</span><span class="sxs-lookup"><span data-stu-id="2e326-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="2e326-319">Wenn die Datei nicht vorhanden ist, erstellt der Code eine neue `Chart` aus einem Array.</span><span class="sxs-lookup"><span data-stu-id="2e326-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="2e326-320">Dieses Mal ruft der Code die `Save`-Methode auf und übergibt den `path`-Parameter, um den Dateipfad und den Dateinamen anzugeben, wo das Diagramm gespeichert werden soll.</span><span class="sxs-lookup"><span data-stu-id="2e326-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="2e326-321">Im Text der Seite verwendet ein `<img>`-Element den Pfad, um auf die anzuzeigende *JPG* -Datei zu zeigen.</span><span class="sxs-lookup"><span data-stu-id="2e326-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="2e326-322">Führen Sie die Datei " *chartsave. cshtml* " aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="2e326-323">Kehren Sie zu webmatrix zurück.</span><span class="sxs-lookup"><span data-stu-id="2e326-323">Return to WebMatrix.</span></span> <span data-ttu-id="2e326-324">Beachten Sie, dass eine Bilddatei mit dem Namen *chart01. jpg* im Ordner *\_chartfiles* gespeichert wurde.</span><span class="sxs-lookup"><span data-stu-id="2e326-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="2e326-325">Speichern eines Diagramms als XML-Datei</span><span class="sxs-lookup"><span data-stu-id="2e326-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="2e326-326">Schließlich können Sie ein Diagramm als XML-Datei auf dem Server speichern.</span><span class="sxs-lookup"><span data-stu-id="2e326-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="2e326-327">Ein Vorteil der Verwendung dieser Methode gegenüber dem Zwischenspeichern des Diagramms oder dem Speichern des Diagramms in einer Datei besteht darin, dass Sie den XML-Code vor dem Anzeigen des Diagramms ändern können, wenn Sie möchten.</span><span class="sxs-lookup"><span data-stu-id="2e326-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="2e326-328">Die Anwendung muss über Lese-/Schreibberechtigungen für den Ordner auf dem Server verfügen, auf dem Sie die Bilddatei ablegen möchten.</span><span class="sxs-lookup"><span data-stu-id="2e326-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="2e326-329">Erstellen Sie im Stammverzeichnis Ihrer Website eine neue Datei mit dem Namen *chartsavexml. cshtml*.</span><span class="sxs-lookup"><span data-stu-id="2e326-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="2e326-330">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="2e326-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="2e326-331">Dieser Code ähnelt dem Code, den Sie bereits gesehen haben, um ein Diagramm im Cache zu speichern, mit dem Unterschied, dass eine XML-Datei verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="2e326-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="2e326-332">Der Code prüft zunächst, ob die XML-Datei vorhanden ist, indem Sie die `File.Exists`-Methode aufrufen.</span><span class="sxs-lookup"><span data-stu-id="2e326-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="2e326-333">Wenn die Datei vorhanden ist, erstellt der Code ein neues `Chart` Objekt und übergibt den Dateinamen als `themePath` Parameter.</span><span class="sxs-lookup"><span data-stu-id="2e326-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="2e326-334">Dadurch wird das Diagramm basierend auf den in der XML-Datei vorhandenen Dateien erstellt.</span><span class="sxs-lookup"><span data-stu-id="2e326-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="2e326-335">Wenn die XML-Datei nicht bereits vorhanden ist, erstellt der Code ein Diagramm wie normal und ruft dann `SaveXml` auf, um es zu speichern.</span><span class="sxs-lookup"><span data-stu-id="2e326-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="2e326-336">Das Diagramm wird mit der `Write`-Methode gerendert, wie Sie dies bereits gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="2e326-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="2e326-337">Wie bei der Seite, die das Caching anzeigt, enthält dieser Code einen Zeitstempel im Diagramm Titel.</span><span class="sxs-lookup"><span data-stu-id="2e326-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="2e326-338">Erstellen Sie eine neue Seite mit dem Namen *chartdisplayxmlchart. cshtml* , und fügen Sie Ihr das folgende Markup hinzu:</span><span class="sxs-lookup"><span data-stu-id="2e326-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="2e326-339">Führen Sie die Seite *chartdisplayxmlchart. cshtml* aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="2e326-340">Das Diagramm wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="2e326-340">The chart is displayed.</span></span> <span data-ttu-id="2e326-341">Notieren Sie sich den Zeitstempel im Titel des Diagramms.</span><span class="sxs-lookup"><span data-stu-id="2e326-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="2e326-342">Schließen Sie den Browser.</span><span class="sxs-lookup"><span data-stu-id="2e326-342">Close the browser.</span></span>
6. <span data-ttu-id="2e326-343">Klicken Sie in webmatrix mit der rechten Maustaste auf den Ordner *\_chartfiles* , klicken Sie auf **Aktualisieren**, und öffnen Sie dann den Ordner.</span><span class="sxs-lookup"><span data-stu-id="2e326-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="2e326-344">Die Datei *xmlchart. XML* in diesem Ordner wurde vom `Chart`-Hilfsprogramm erstellt.</span><span class="sxs-lookup"><span data-stu-id="2e326-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Beschreibung: der _ChartFiles Ordner, der die vom Diagramm Hilfsprogramm erstellte Datei "xmlchart. xml" anzeigt.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="2e326-346">Führen Sie die Seite *chartdisplayxmlchart. cshtml* erneut aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="2e326-347">Das Diagramm zeigt den gleichen Zeitstempel an, als Sie die Seite zum ersten Mal ausgeführt haben.</span><span class="sxs-lookup"><span data-stu-id="2e326-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="2e326-348">Das liegt daran, dass das Diagramm aus dem XML-Code generiert wird, den Sie zuvor gespeichert haben.</span><span class="sxs-lookup"><span data-stu-id="2e326-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="2e326-349">Öffnen Sie in webmatrix den *\_chartfiles* -Ordner, und löschen Sie die Datei " *xmlchart. XML* ".</span><span class="sxs-lookup"><span data-stu-id="2e326-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="2e326-350">Führen Sie die Seite " *chartdisplayxmlchart. cshtml* " erneut aus.</span><span class="sxs-lookup"><span data-stu-id="2e326-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="2e326-351">Dieses Mal wird der Zeitstempel aktualisiert, da der `Chart`-Hilfsprogramm die XML-Datei neu erstellen musste.</span><span class="sxs-lookup"><span data-stu-id="2e326-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="2e326-352">Wenn Sie möchten, überprüfen Sie den *\_chartfiles* -Ordner, und beachten Sie, dass die XML-Datei zurück ist.</span><span class="sxs-lookup"><span data-stu-id="2e326-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="2e326-353">Zusätzliche Ressourcen</span><span class="sxs-lookup"><span data-stu-id="2e326-353">Additional Resources</span></span>

- [<span data-ttu-id="2e326-354">Einführung in das Arbeiten mit einer Datenbank an ASP.net Web Pages Websites</span><span class="sxs-lookup"><span data-stu-id="2e326-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="2e326-355">Verwenden der Zwischenspeicherung in ASP.net Web Pages Websites zum Verbessern der Leistung</span><span class="sxs-lookup"><span data-stu-id="2e326-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="2e326-356">[Chart-Klasse](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.net Web Pages-API-Referenz auf MSDN)</span><span class="sxs-lookup"><span data-stu-id="2e326-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
