---
uid: mvc/overview/older-versions-1/controllers-and-routing/aspnet-mvc-controllers-overview-cs
title: ASP.NET MVC-Controller ÜbersichtC#() | Microsoft-Dokumentation
author: StephenWalther
description: In diesem Tutorial führt Stephen Walther Sie in ASP.NET MVC-Controller ein. Sie erfahren, wie Sie neue Controller erstellen und verschiedene Arten von Aktionen zurückgeben...
ms.author: riande
ms.date: 02/16/2008
ms.assetid: b985c49a-3668-455c-a366-f85f6bc64b12
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/aspnet-mvc-controllers-overview-cs
msc.type: authoredcontent
ms.openlocfilehash: 1a287b37742400a17c2ed53cfd00bfb053b4f3d2
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 03/06/2020
ms.locfileid: "78437679"
---
# <a name="aspnet-mvc-controller-overview-c"></a><span data-ttu-id="a4ba1-104">ASP.NET MVC-Controller – Übersicht (C#)</span><span class="sxs-lookup"><span data-stu-id="a4ba1-104">ASP.NET MVC Controller Overview (C#)</span></span>

<span data-ttu-id="a4ba1-105">von [Stephen Walther](https://github.com/StephenWalther)</span><span class="sxs-lookup"><span data-stu-id="a4ba1-105">by [Stephen Walther](https://github.com/StephenWalther)</span></span>

> <span data-ttu-id="a4ba1-106">In diesem Tutorial führt Stephen Walther Sie in ASP.NET MVC-Controller ein.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-106">In this tutorial, Stephen Walther introduces you to ASP.NET MVC controllers.</span></span> <span data-ttu-id="a4ba1-107">Sie erfahren, wie Sie neue Controller erstellen und unterschiedliche Aktions Ergebnistypen zurückgeben.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-107">You learn how to create new controllers and return different types of action results.</span></span>

<span data-ttu-id="a4ba1-108">In diesem Tutorial wird das Thema ASP.NET MVC-Controller, Controller Aktionen und Aktions Ergebnisse erläutert.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-108">This tutorial explores the topic of ASP.NET MVC controllers, controller actions, and action results.</span></span> <span data-ttu-id="a4ba1-109">Nach Abschluss dieses Tutorials erfahren Sie, wie Controller verwendet werden, um zu steuern, wie ein Besucher mit einer ASP.NET MVC-Website interagiert.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-109">After you complete this tutorial, you will understand how controllers are used to control the way a visitor interacts with an ASP.NET MVC website.</span></span>

## <a name="understanding-controllers"></a><span data-ttu-id="a4ba1-110">Grundlagen der Controller</span><span class="sxs-lookup"><span data-stu-id="a4ba1-110">Understanding Controllers</span></span>

<span data-ttu-id="a4ba1-111">MVC-Controller sind dafür verantwortlich, auf Anforderungen zu reagieren, die für eine ASP.NET MVC-Website gestellt werden.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-111">MVC controllers are responsible for responding to requests made against an ASP.NET MVC website.</span></span> <span data-ttu-id="a4ba1-112">Jede Browser Anforderung wird einem bestimmten Controller zugeordnet.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-112">Each browser request is mapped to a particular controller.</span></span> <span data-ttu-id="a4ba1-113">Stellen Sie sich beispielsweise vor, dass Sie die folgende URL in die Adressleiste Ihres Browsers eingeben:</span><span class="sxs-lookup"><span data-stu-id="a4ba1-113">For example, imagine that you enter the following URL into the address bar of your browser:</span></span>

`http://localhost/Product/Index/3`

<span data-ttu-id="a4ba1-114">In diesem Fall wird ein Controller mit dem Namen ProductController aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-114">In this case, a controller named ProductController is invoked.</span></span> <span data-ttu-id="a4ba1-115">Der ProductController ist dafür verantwortlich, die Antwort auf die Browser Anforderung zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-115">The ProductController is responsible for generating the response to the browser request.</span></span> <span data-ttu-id="a4ba1-116">Beispielsweise kann der Controller eine bestimmte Ansicht zurück an den Browser zurückgeben oder der Controller kann den Benutzer an einen anderen Controller umleiten.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-116">For example, the controller might return a particular view back to the browser or the controller might redirect the user to another controller.</span></span>

<span data-ttu-id="a4ba1-117">Die Auflistung 1 enthält einen einfachen Controller mit dem Namen ProductController.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-117">Listing 1 contains a simple controller named ProductController.</span></span>

<span data-ttu-id="a4ba1-118">**Listing1-controllers\productcontroller.cs**</span><span class="sxs-lookup"><span data-stu-id="a4ba1-118">**Listing1 - Controllers\ProductController.cs**</span></span>

[!code-csharp[Main](aspnet-mvc-controllers-overview-cs/samples/sample1.cs)]

<span data-ttu-id="a4ba1-119">Wie Sie in der Liste 1 sehen können, ist ein Controller nur eine Klasse (ein Visual Basic .net C# oder eine Klasse).</span><span class="sxs-lookup"><span data-stu-id="a4ba1-119">As you can see from Listing 1, a controller is just a class (a Visual Basic .NET or C# class).</span></span> <span data-ttu-id="a4ba1-120">Ein Controller ist eine Klasse, die von der System. Web. MVC. Controller-Basisklasse abgeleitet wird.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-120">A controller is a class that derives from the base System.Web.Mvc.Controller class.</span></span> <span data-ttu-id="a4ba1-121">Da ein Controller von dieser Basisklasse erbt, erbt ein Controller mehrere nützliche Methoden kostenlos (diese Methoden werden in Kürze besprochen).</span><span class="sxs-lookup"><span data-stu-id="a4ba1-121">Because a controller inherits from this base class, a controller inherits several useful methods for free (We discuss these methods in a moment).</span></span>

## <a name="understanding-controller-actions"></a><span data-ttu-id="a4ba1-122">Grundlegendes zu Controller Aktionen</span><span class="sxs-lookup"><span data-stu-id="a4ba1-122">Understanding Controller Actions</span></span>

<span data-ttu-id="a4ba1-123">Controller Aktionen werden von einem Controller bereitstellt.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-123">A controller exposes controller actions.</span></span> <span data-ttu-id="a4ba1-124">Eine Aktion ist eine Methode auf einem Controller, die aufgerufen wird, wenn Sie eine bestimmte URL in der Adressleiste Ihres Browsers eingeben.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-124">An action is a method on a controller that gets called when you enter a particular URL in your browser address bar.</span></span> <span data-ttu-id="a4ba1-125">Stellen Sie sich beispielsweise vor, dass Sie eine Anforderung für die folgende URL senden:</span><span class="sxs-lookup"><span data-stu-id="a4ba1-125">For example, imagine that you make a request for the following URL:</span></span>

`http://localhost/Product/Index/3`

<span data-ttu-id="a4ba1-126">In diesem Fall wird die Index ()-Methode für die ProductController-Klasse aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-126">In this case, the Index() method is called on the ProductController class.</span></span> <span data-ttu-id="a4ba1-127">Die Index ()-Methode ist ein Beispiel für eine Controller Aktion.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-127">The Index() method is an example of a controller action.</span></span>

<span data-ttu-id="a4ba1-128">Eine Controller Aktion muss eine öffentliche Methode einer Controller Klasse sein.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-128">A controller action must be a public method of a controller class.</span></span> <span data-ttu-id="a4ba1-129">C#Standardmäßig sind Methoden private Methoden.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-129">C# methods, by default, are private methods.</span></span> <span data-ttu-id="a4ba1-130">Beachten Sie, dass jede öffentliche Methode, die Sie einer Controller Klasse hinzufügen, automatisch als Controller Aktion verfügbar gemacht wird (Sie müssen darauf achten, da eine Controller Aktion von beliebigen Personen im Universum aufgerufen werden kann, indem Sie einfach die richtige URL in eine Adressleiste des Browsers eingeben).</span><span class="sxs-lookup"><span data-stu-id="a4ba1-130">Realize that any public method that you add to a controller class is exposed as a controller action automatically (You must be careful about this since a controller action can be invoked by anyone in the universe simply by typing the right URL into a browser address bar).</span></span>

<span data-ttu-id="a4ba1-131">Es gibt einige zusätzliche Anforderungen, die durch eine Controller Aktion erfüllt werden müssen.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-131">There are some additional requirements that must be satisfied by a controller action.</span></span> <span data-ttu-id="a4ba1-132">Eine Methode, die als Controller Aktion verwendet wird, kann nicht überladen werden.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-132">A method used as a controller action cannot be overloaded.</span></span> <span data-ttu-id="a4ba1-133">Außerdem kann eine Controller Aktion keine statische Methode sein.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-133">Furthermore, a controller action cannot be a static method.</span></span> <span data-ttu-id="a4ba1-134">Abgesehen davon können Sie fast jede Methode als Controller Aktion verwenden.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-134">Other than that, you can use just about any method as a controller action.</span></span>

## <a name="understanding-action-results"></a><span data-ttu-id="a4ba1-135">Grundlegendes zu Aktions Ergebnissen</span><span class="sxs-lookup"><span data-stu-id="a4ba1-135">Understanding Action Results</span></span>

<span data-ttu-id="a4ba1-136">Eine Controller Aktion gibt etwas zurück, das als *Aktions Ergebnis*bezeichnet wird.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-136">A controller action returns something called an *action result*.</span></span> <span data-ttu-id="a4ba1-137">Ein Aktions Ergebnis ist, was eine Controller Aktion als Reaktion auf eine Browser Anforderung zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-137">An action result is what a controller action returns in response to a browser request.</span></span>

<span data-ttu-id="a4ba1-138">Das ASP.NET-MVC-Framework unterstützt verschiedene Arten von Aktions Ergebnissen, einschließlich:</span><span class="sxs-lookup"><span data-stu-id="a4ba1-138">The ASP.NET MVC framework supports several types of action results including:</span></span>

1. <span data-ttu-id="a4ba1-139">ViewResult-stellt HTML und Markup dar.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-139">ViewResult - Represents HTML and markup.</span></span>
2. <span data-ttu-id="a4ba1-140">EmptyResult-stellt kein Ergebnis dar.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-140">EmptyResult - Represents no result.</span></span>
3. <span data-ttu-id="a4ba1-141">Redirectresult: stellt eine Umleitung zu einer neuen URL dar.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-141">RedirectResult - Represents a redirection to a new URL.</span></span>
4. <span data-ttu-id="a4ba1-142">Jsonresult: stellt ein JavaScript Object Notation Ergebnis dar, das in einer AJAX-Anwendung verwendet werden kann.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-142">JsonResult - Represents a JavaScript Object Notation result that can be used in an AJAX application.</span></span>
5. <span data-ttu-id="a4ba1-143">Javascriptresult: stellt ein JavaScript-Skript dar.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-143">JavaScriptResult - Represents a JavaScript script.</span></span>
6. <span data-ttu-id="a4ba1-144">ContentResult-stellt ein Text Ergebnis dar.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-144">ContentResult - Represents a text result.</span></span>
7. <span data-ttu-id="a4ba1-145">Filecontentresult: stellt eine herunterladbare Datei dar (mit dem binären Inhalt).</span><span class="sxs-lookup"><span data-stu-id="a4ba1-145">FileContentResult - Represents a downloadable file (with the binary content).</span></span>
8. <span data-ttu-id="a4ba1-146">Fileblothresult: stellt eine herunterladbare Datei (mit einem Pfad) dar.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-146">FilePathResult - Represents a downloadable file (with a path).</span></span>
9. <span data-ttu-id="a4ba1-147">Filestreamresult: stellt eine herunterladbare Datei (mit einem Dateistream) dar.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-147">FileStreamResult - Represents a downloadable file (with a file stream).</span></span>

<span data-ttu-id="a4ba1-148">Alle diese Aktions Ergebnisse erben von der Basisklasse "action result".</span><span class="sxs-lookup"><span data-stu-id="a4ba1-148">All of these action results inherit from the base ActionResult class.</span></span>

<span data-ttu-id="a4ba1-149">In den meisten Fällen gibt eine Controller Aktion ein ViewResult zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-149">In most cases, a controller action returns a ViewResult.</span></span> <span data-ttu-id="a4ba1-150">Die Index Controller Aktion in der Liste 2 gibt beispielsweise ein ViewResult zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-150">For example, the Index controller action in Listing 2 returns a ViewResult.</span></span>

<span data-ttu-id="a4ba1-151">**Codebeispiel 2: controllers\bookcontroller.cs**</span><span class="sxs-lookup"><span data-stu-id="a4ba1-151">**Listing 2 - Controllers\BookController.cs**</span></span>

[!code-csharp[Main](aspnet-mvc-controllers-overview-cs/samples/sample2.cs)]

<span data-ttu-id="a4ba1-152">Wenn eine Aktion ein ViewResult zurückgibt, wird HTML an den Browser zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-152">When an action returns a ViewResult, HTML is returned to the browser.</span></span> <span data-ttu-id="a4ba1-153">Die Index ()-Methode in der Liste 2 gibt eine Sicht mit dem Namen Index für den Browser zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-153">The Index() method in Listing 2 returns a view named Index to the browser.</span></span>

<span data-ttu-id="a4ba1-154">Beachten Sie, dass die Index ()-Aktion in der Liste 2 kein ViewResult () zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-154">Notice that the Index() action in Listing 2 does not return a ViewResult().</span></span> <span data-ttu-id="a4ba1-155">Stattdessen wird die View ()-Methode der Controller-Basisklasse aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-155">Instead, the View() method of the Controller base class is called.</span></span> <span data-ttu-id="a4ba1-156">Normalerweise wird ein Aktions Ergebnis nicht direkt zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-156">Normally, you do not return an action result directly.</span></span> <span data-ttu-id="a4ba1-157">Stattdessen wird eine der folgenden Methoden der Controller-Basisklasse aufgerufen:</span><span class="sxs-lookup"><span data-stu-id="a4ba1-157">Instead, you call one of the following methods of the Controller base class:</span></span>

1. <span data-ttu-id="a4ba1-158">View: gibt ein Ergebnis der ViewResult-Aktion zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-158">View - Returns a ViewResult action result.</span></span>
2. <span data-ttu-id="a4ba1-159">Redirect-gibt ein redirectresult-Aktions Ergebnis zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-159">Redirect - Returns a RedirectResult action result.</span></span>
3. <span data-ttu-id="a4ba1-160">Redirectdeaction: gibt ein redirecttorouteresult-Aktions Ergebnis zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-160">RedirectToAction - Returns a RedirectToRouteResult action result.</span></span>
4. <span data-ttu-id="a4ba1-161">RedirectToRoute-gibt ein redirecttorouteresult-Aktions Ergebnis zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-161">RedirectToRoute - Returns a RedirectToRouteResult action result.</span></span>
5. <span data-ttu-id="a4ba1-162">JSON: gibt ein jsonresult-Aktions Ergebnis zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-162">Json - Returns a JsonResult action result.</span></span>
6. <span data-ttu-id="a4ba1-163">Javascriptresult: gibt einen javascriptresult zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-163">JavaScriptResult - Returns a JavaScriptResult.</span></span>
7. <span data-ttu-id="a4ba1-164">Content: gibt ein Ergebnis der ContentResult-Aktion zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-164">Content - Returns a ContentResult action result.</span></span>
8. <span data-ttu-id="a4ba1-165">File: gibt ein filecontentresult, filepthresult oder filestreamresult zurück, abhängig von den Parametern, die an die Methode weitergegeben werden.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-165">File - Returns a FileContentResult, FilePathResult, or FileStreamResult depending on the parameters passed to the method.</span></span>

<span data-ttu-id="a4ba1-166">Wenn Sie also eine Ansicht an den Browser zurückgeben möchten, wird die View ()-Methode aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-166">So, if you want to return a View to the browser, you call the View() method.</span></span> <span data-ttu-id="a4ba1-167">Wenn Sie den Benutzer von einer Controller Aktion zu einer anderen umleiten möchten, können Sie die redirectdeaction ()-Methode aufzurufen.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-167">If you want to redirect the user from one controller action to another, you call the RedirectToAction() method.</span></span> <span data-ttu-id="a4ba1-168">Beispielsweise wird in der Aktion Details () in der Liste 3 entweder eine Ansicht angezeigt, oder der Benutzer wird zur Index ()-Aktion umgeleitet, je nachdem, ob der ID-Parameter einen Wert aufweist.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-168">For example, the Details() action in Listing 3 either displays a view or redirects the user to the Index() action depending on whether the Id parameter has a value.</span></span>

<span data-ttu-id="a4ba1-169">**Codebeispiel 3-CustomerController.cs**</span><span class="sxs-lookup"><span data-stu-id="a4ba1-169">**Listing 3 - CustomerController.cs**</span></span>

[!code-csharp[Main](aspnet-mvc-controllers-overview-cs/samples/sample3.cs)]

<span data-ttu-id="a4ba1-170">Das Ergebnis der ContentResult-Aktion ist ein besonderes Ergebnis.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-170">The ContentResult action result is special.</span></span> <span data-ttu-id="a4ba1-171">Sie können das Ergebnis der ContentResult-Aktion verwenden, um ein Aktions Ergebnis als nur-Text zurückzugeben.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-171">You can use the ContentResult action result to return an action result as plain text.</span></span> <span data-ttu-id="a4ba1-172">Beispielsweise gibt die Index ()-Methode in der Auflistung 4 eine Meldung als nur-Text und nicht als HTML zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-172">For example, the Index() method in Listing 4 returns a message as plain text and not as HTML.</span></span>

<span data-ttu-id="a4ba1-173">**Codebeispiel 4-controllers\status Controller .cs**</span><span class="sxs-lookup"><span data-stu-id="a4ba1-173">**Listing 4 - Controllers\StatusController.cs**</span></span>

[!code-csharp[Main](aspnet-mvc-controllers-overview-cs/samples/sample4.cs)]

<span data-ttu-id="a4ba1-174">Wenn die Status Controller. Index ()-Aktion aufgerufen wird, wird eine Sicht nicht zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-174">When the StatusController.Index() action is invoked, a view is not returned.</span></span> <span data-ttu-id="a4ba1-175">Stattdessen wird der Rohtext "Hallo Welt!"</span><span class="sxs-lookup"><span data-stu-id="a4ba1-175">Instead, the raw text "Hello World!"</span></span> <span data-ttu-id="a4ba1-176">wird an den Browser zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-176">is returned to the browser.</span></span>

<span data-ttu-id="a4ba1-177">Wenn eine Controller Aktion ein Ergebnis zurückgibt, das kein Aktions Ergebnis ist (z. b. ein Datum oder eine ganze Zahl), wird das Ergebnis automatisch in ein ContentResult umschließt.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-177">If a controller action returns a result that is not an action result - for example, a date or an integer - then the result is wrapped in a ContentResult automatically.</span></span> <span data-ttu-id="a4ba1-178">Wenn z. b. die Index ()-Aktion des Arbeits Controllers in der Auflistung 5 aufgerufen wird, wird das Datum automatisch als ContentResult zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-178">For example, when the Index() action of the WorkController in Listing 5 is invoked, the date is returned as a ContentResult automatically.</span></span>

<span data-ttu-id="a4ba1-179">**Auflistung 5-WorkController.cs**</span><span class="sxs-lookup"><span data-stu-id="a4ba1-179">**Listing 5 - WorkController.cs**</span></span>

[!code-csharp[Main](aspnet-mvc-controllers-overview-cs/samples/sample5.cs)]

<span data-ttu-id="a4ba1-180">Die Index ()-Aktion in Auflistung 5 gibt ein DateTime-Objekt zurück.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-180">The Index() action in Listing 5 returns a DateTime object.</span></span> <span data-ttu-id="a4ba1-181">Das ASP.NET-MVC-Framework konvertiert das DateTime-Objekt in eine Zeichenfolge und umschließt den DateTime-Wert in einem ContentResult automatisch.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-181">The ASP.NET MVC framework converts the DateTime object to a string and wraps the DateTime value in a ContentResult automatically.</span></span> <span data-ttu-id="a4ba1-182">Der Browser empfängt das Datum und die Uhrzeit als Klartext.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-182">The browser receives the date and time as plain text.</span></span>

## <a name="summary"></a><span data-ttu-id="a4ba1-183">Zusammenfassung</span><span class="sxs-lookup"><span data-stu-id="a4ba1-183">Summary</span></span>

<span data-ttu-id="a4ba1-184">In diesem Tutorial wurden die Konzepte der ASP.NET MVC-Controller, Controller Aktionen und Controller Aktions Ergebnisse vorgestellt.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-184">The purpose of this tutorial was to introduce you to the concepts of ASP.NET MVC controllers, controller actions, and controller action results.</span></span> <span data-ttu-id="a4ba1-185">Im ersten Abschnitt haben Sie erfahren, wie Sie einem ASP.NET MVC-Projekt neue Controller hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-185">In the first section, you learned how to add new controllers to an ASP.NET MVC project.</span></span> <span data-ttu-id="a4ba1-186">Als nächstes haben Sie gelernt, wie öffentliche Methoden eines Controllers als Controller Aktionen für das Universum verfügbar gemacht werden.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-186">Next, you learned how public methods of a controller are exposed to the universe as controller actions.</span></span> <span data-ttu-id="a4ba1-187">Schließlich haben wir die unterschiedlichen Arten von Aktions Ergebnissen erläutert, die von einer Controller Aktion zurückgegeben werden können.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-187">Finally, we discussed the different types of action results that can be returned from a controller action.</span></span> <span data-ttu-id="a4ba1-188">Insbesondere haben wir erläutert, wie ViewResult, redirectdeaktionresult und ContentResult aus einer Controller Aktion zurückgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="a4ba1-188">In particular, we discussed how to return a ViewResult, RedirectToActionResult, and ContentResult from a controller action.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="a4ba1-189">[Zurück](creating-an-action-vb.md)
> [Weiter](creating-custom-routes-cs.md)</span><span class="sxs-lookup"><span data-stu-id="a4ba1-189">[Previous](creating-an-action-vb.md)
[Next](creating-custom-routes-cs.md)</span></span>
