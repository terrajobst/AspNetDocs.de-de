---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
title: Grundlegendes zu AktionsC#Filtern () | Microsoft-Dokumentation
author: microsoft
description: Ziel dieses Tutorials ist es, Aktionsfilter zu erläutern. Ein Aktionsfilter ist ein Attribut, das Sie auf eine Controller Aktion anwenden können, oder auf einen vollständigen Controller...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: a94e4e81-40c1-47b7-8613-126a1a6cc93d
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-cs
msc.type: authoredcontent
ms.openlocfilehash: d1c72c2355c6122f851351a8c1e8f04fa63ae04e
ms.sourcegitcommit: 22fbd8863672c4ad6693b8388ad5c8e753fb41a2
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 11/28/2019
ms.locfileid: "74590103"
---
# <a name="understanding-action-filters-c"></a><span data-ttu-id="a621e-104">Grundlegendes zu Aktionsfiltern (C#)</span><span class="sxs-lookup"><span data-stu-id="a621e-104">Understanding Action Filters (C#)</span></span>

<span data-ttu-id="a621e-105">von [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="a621e-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="a621e-106">PDF herunterladen</span><span class="sxs-lookup"><span data-stu-id="a621e-106">Download PDF</span></span>](https://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_CS.pdf)

> <span data-ttu-id="a621e-107">Ziel dieses Tutorials ist es, Aktionsfilter zu erläutern.</span><span class="sxs-lookup"><span data-stu-id="a621e-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="a621e-108">Ein Aktionsfilter ist ein Attribut, das Sie auf eine Controller Aktion anwenden können, oder ein ganzer Controller, der die Art und Weise ändert, in der die Aktion ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="a621e-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>

## <a name="understanding-action-filters"></a><span data-ttu-id="a621e-109">Grundlegendes zu Aktions Filtern</span><span class="sxs-lookup"><span data-stu-id="a621e-109">Understanding Action Filters</span></span>

<span data-ttu-id="a621e-110">Ziel dieses Tutorials ist es, Aktionsfilter zu erläutern.</span><span class="sxs-lookup"><span data-stu-id="a621e-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="a621e-111">Ein Aktionsfilter ist ein Attribut, das Sie auf eine Controller Aktion anwenden können, oder ein ganzer Controller, der die Art und Weise ändert, in der die Aktion ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="a621e-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="a621e-112">Das ASP.NET-MVC-Framework umfasst mehrere Aktionsfilter:</span><span class="sxs-lookup"><span data-stu-id="a621e-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="a621e-113">OutputCache – dieser Aktionsfilter speichert die Ausgabe einer Controller Aktion für einen angegebenen Zeitraum zwischen.</span><span class="sxs-lookup"><span data-stu-id="a621e-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="a621e-114">Lenker Error – dieser Aktionsfilter behandelt Fehler, die ausgelöst werden, wenn eine Controller Aktion ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="a621e-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="a621e-115">Autorisieren – dieser Aktionsfilter ermöglicht Ihnen, den Zugriff auf einen bestimmten Benutzer oder eine Rolle einzuschränken.</span><span class="sxs-lookup"><span data-stu-id="a621e-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="a621e-116">Sie können auch eigene benutzerdefinierte Aktionsfilter erstellen.</span><span class="sxs-lookup"><span data-stu-id="a621e-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="a621e-117">Beispielsweise können Sie einen benutzerdefinierten Aktionsfilter erstellen, um ein benutzerdefiniertes Authentifizierungssystem zu implementieren.</span><span class="sxs-lookup"><span data-stu-id="a621e-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="a621e-118">Oder Sie möchten einen Aktionsfilter erstellen, der die von einer Controller Aktion zurückgegebenen Ansichts Daten ändert.</span><span class="sxs-lookup"><span data-stu-id="a621e-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="a621e-119">In diesem Tutorial erfahren Sie, wie Sie einen Aktionsfilter von Grund auf erstellen.</span><span class="sxs-lookup"><span data-stu-id="a621e-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="a621e-120">Wir erstellen einen Protokoll Aktionsfilter, mit dem unterschiedliche Phasen der Verarbeitung einer Aktion im Visual Studio-Ausgabefenster protokolliert werden.</span><span class="sxs-lookup"><span data-stu-id="a621e-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="a621e-121">Verwenden eines Aktions Filters</span><span class="sxs-lookup"><span data-stu-id="a621e-121">Using an Action Filter</span></span>

<span data-ttu-id="a621e-122">Ein Aktionsfilter ist ein Attribut.</span><span class="sxs-lookup"><span data-stu-id="a621e-122">An action filter is an attribute.</span></span> <span data-ttu-id="a621e-123">Sie können die meisten Aktionsfilter entweder auf eine einzelne Controller Aktion oder einen gesamten Controller anwenden.</span><span class="sxs-lookup"><span data-stu-id="a621e-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="a621e-124">Beispielsweise macht der Daten Controller in der Liste 1 eine Aktion mit dem Namen `Index()` verfügbar, die die aktuelle Zeit zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="a621e-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="a621e-125">Diese Aktion ist mit dem Filter für die `OutputCache` Aktion versehen.</span><span class="sxs-lookup"><span data-stu-id="a621e-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="a621e-126">Dieser Filter bewirkt, dass der von der Aktion zurückgegebene Wert 10 Sekunden lang zwischengespeichert wird.</span><span class="sxs-lookup"><span data-stu-id="a621e-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="a621e-127">**Codebeispiel 1 – `Controllers\DataController.cs`**</span><span class="sxs-lookup"><span data-stu-id="a621e-127">**Listing 1 – `Controllers\DataController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample1.cs)]

<span data-ttu-id="a621e-128">Wenn Sie die `Index()` Aktion wiederholt aufrufen, indem Sie die URL/Data/Index in die Adressleiste Ihres Browsers eingeben und mehrmals auf die Schaltfläche "Aktualisieren" klicken, wird die gleiche Zeit für 10 Sekunden angezeigt.</span><span class="sxs-lookup"><span data-stu-id="a621e-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="a621e-129">Die Ausgabe der `Index()` Aktion wird 10 Sekunden lang zwischengespeichert (siehe Abbildung 1).</span><span class="sxs-lookup"><span data-stu-id="a621e-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>

<span data-ttu-id="a621e-130">[![zwischengespeicherte Zeit](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="a621e-130">[![Cached time](understanding-action-filters-cs/_static/image2.png)](understanding-action-filters-cs/_static/image1.png)</span></span>

<span data-ttu-id="a621e-131">**Abbildung 01**: zwischengespeicherte Zeit ([Klicken Sie, um das Bild in voller Größe anzuzeigen](understanding-action-filters-cs/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="a621e-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-cs/_static/image3.png))</span></span>

<span data-ttu-id="a621e-132">In der Liste 1 wird ein einzelner Aktionsfilter – der `OutputCache` Aktionsfilter – auf die `Index()`-Methode angewendet.</span><span class="sxs-lookup"><span data-stu-id="a621e-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="a621e-133">Wenn Sie benötigen, können Sie mehrere Aktionsfilter auf dieselbe Aktion anwenden.</span><span class="sxs-lookup"><span data-stu-id="a621e-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="a621e-134">Beispielsweise können Sie die `OutputCache`-und `HandleError` Aktionsfilter auf dieselbe Aktion anwenden.</span><span class="sxs-lookup"><span data-stu-id="a621e-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="a621e-135">In der Liste 1 wird der `OutputCache` Aktionsfilter auf die `Index()` Aktion angewendet.</span><span class="sxs-lookup"><span data-stu-id="a621e-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="a621e-136">Sie können dieses Attribut auch auf die `DataController` Klasse selbst anwenden.</span><span class="sxs-lookup"><span data-stu-id="a621e-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="a621e-137">In diesem Fall würde das Ergebnis, das von einer Aktion zurückgegeben wird, die vom Controller verfügbar gemacht wird, 10 Sekunden lang zwischengespeichert werden.</span><span class="sxs-lookup"><span data-stu-id="a621e-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="a621e-138">Die verschiedenen Filtertypen</span><span class="sxs-lookup"><span data-stu-id="a621e-138">The Different Types of Filters</span></span>

<span data-ttu-id="a621e-139">Das ASP.NET-MVC-Framework unterstützt vier verschiedene Arten von Filtern:</span><span class="sxs-lookup"><span data-stu-id="a621e-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="a621e-140">Autorisierungs Filter – implementiert das `IAuthorizationFilter`-Attribut.</span><span class="sxs-lookup"><span data-stu-id="a621e-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="a621e-141">Aktionsfilter – implementiert das `IActionFilter`-Attribut.</span><span class="sxs-lookup"><span data-stu-id="a621e-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="a621e-142">Ergebnis Filter – implementiert das `IResultFilter`-Attribut.</span><span class="sxs-lookup"><span data-stu-id="a621e-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="a621e-143">Ausnahme Filter – implementiert das `IExceptionFilter`-Attribut.</span><span class="sxs-lookup"><span data-stu-id="a621e-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="a621e-144">Filter werden in der oben aufgeführten Reihenfolge ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="a621e-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="a621e-145">Autorisierungs Filter werden z. b. immer ausgeführt, bevor Aktionsfilter und Ausnahme Filter immer nach jedem anderen Filtertyp ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="a621e-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="a621e-146">Autorisierungs Filter werden verwendet, um die Authentifizierung und Autorisierung für Controller Aktionen zu implementieren.</span><span class="sxs-lookup"><span data-stu-id="a621e-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="a621e-147">Der Autorisierungs Filter ist z. b. ein Beispiel für einen Autorisierungs Filter.</span><span class="sxs-lookup"><span data-stu-id="a621e-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="a621e-148">Aktionsfilter enthalten Logik, die vor und nach der Ausführung einer Controller Aktion ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="a621e-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="a621e-149">Sie können beispielsweise einen Aktionsfilter verwenden, um die Ansichts Daten zu ändern, die von einer Controller Aktion zurückgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="a621e-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="a621e-150">Ergebnis Filter enthalten Logik, die vor und nach dem Ausführen eines Ansichts Ergebnisses ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="a621e-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="a621e-151">Beispielsweise möchten Sie möglicherweise ein Ansichts Ergebnis ändern, bevor die Ansicht im Browser gerendert wird.</span><span class="sxs-lookup"><span data-stu-id="a621e-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="a621e-152">Ausnahme Filter sind der letzte Typ des zu testenden Filters.</span><span class="sxs-lookup"><span data-stu-id="a621e-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="a621e-153">Sie können einen Ausnahme Filter verwenden, um Fehler zu behandeln, die entweder von den Controller Aktionen oder von Controller Aktions Ergebnissen ausgelöst werden.</span><span class="sxs-lookup"><span data-stu-id="a621e-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="a621e-154">Sie können auch Ausnahme Filter verwenden, um Fehler zu protokollieren.</span><span class="sxs-lookup"><span data-stu-id="a621e-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="a621e-155">Jeder andere Filtertyp wird in einer bestimmten Reihenfolge ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="a621e-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="a621e-156">Wenn Sie die Reihenfolge steuern möchten, in der Filter desselben Typs ausgeführt werden, können Sie die Order-Eigenschaft eines Filters festlegen.</span><span class="sxs-lookup"><span data-stu-id="a621e-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="a621e-157">Die Basisklasse für alle Aktionsfilter ist die `System.Web.Mvc.FilterAttribute`-Klasse.</span><span class="sxs-lookup"><span data-stu-id="a621e-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="a621e-158">Wenn Sie einen bestimmten Filtertyp implementieren möchten, müssen Sie eine Klasse erstellen, die von der Basis Filterklasse erbt und eine oder mehrere der Schnittstellen `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`oder `IExceptionFilter` implementiert.</span><span class="sxs-lookup"><span data-stu-id="a621e-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the `IAuthorizationFilter`, `IActionFilter`, `IResultFilter`, or `IExceptionFilter` interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="a621e-159">Die Base Action Filter Attribute-Klasse</span><span class="sxs-lookup"><span data-stu-id="a621e-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="a621e-160">Um Ihnen die Implementierung eines benutzerdefinierten Aktions Filters zu erleichtern, enthält das ASP.NET-MVC-Framework eine Basis `ActionFilterAttribute`-Klasse.</span><span class="sxs-lookup"><span data-stu-id="a621e-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="a621e-161">Diese Klasse implementiert die `IActionFilter`-und `IResultFilter` Schnittstellen und erbt von der `Filter`-Klasse.</span><span class="sxs-lookup"><span data-stu-id="a621e-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="a621e-162">Die Terminologie ist hier nicht vollständig konsistent.</span><span class="sxs-lookup"><span data-stu-id="a621e-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="a621e-163">Technisch gesehen ist eine Klasse, die von der Action Filter Attribute-Klasse erbt, sowohl ein Aktionsfilter als auch ein Ergebnis Filter.</span><span class="sxs-lookup"><span data-stu-id="a621e-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="a621e-164">Allerdings wird der Word-Aktionsfilter verwendet, um auf einen beliebigen Filtertyp im ASP.NET MVC-Framework zu verweisen.</span><span class="sxs-lookup"><span data-stu-id="a621e-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="a621e-165">Die Basis `ActionFilterAttribute` Klasse verfügt über die folgenden Methoden, die Sie überschreiben können:</span><span class="sxs-lookup"><span data-stu-id="a621e-165">The base `ActionFilterAttribute` class has the following methods that you can override:</span></span>

- <span data-ttu-id="a621e-166">OnAction-Ausführung – diese Methode wird aufgerufen, bevor eine Controller Aktion ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="a621e-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="a621e-167">OnAction-– diese Methode wird aufgerufen, nachdem eine Controller Aktion ausgeführt wurde.</span><span class="sxs-lookup"><span data-stu-id="a621e-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="a621e-168">OnResultExecuting – diese Methode wird aufgerufen, bevor ein Controller Aktions Ergebnis ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="a621e-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="a621e-169">OnResultExecuted – diese Methode wird aufgerufen, nachdem ein Controller Aktions Ergebnis ausgeführt wurde.</span><span class="sxs-lookup"><span data-stu-id="a621e-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="a621e-170">Im nächsten Abschnitt erfahren Sie, wie Sie jede dieser unterschiedlichen Methoden implementieren können.</span><span class="sxs-lookup"><span data-stu-id="a621e-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="a621e-171">Erstellen eines Protokoll Aktions Filters</span><span class="sxs-lookup"><span data-stu-id="a621e-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="a621e-172">Um zu veranschaulichen, wie Sie einen benutzerdefinierten Aktionsfilter erstellen können, erstellen wir einen benutzerdefinierten Aktionsfilter, mit dem die Phasen der Verarbeitung einer Controller Aktion im Visual Studio-Ausgabefenster protokolliert werden.</span><span class="sxs-lookup"><span data-stu-id="a621e-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="a621e-173">Unsere `LogActionFilter` ist in der Liste 2 enthalten.</span><span class="sxs-lookup"><span data-stu-id="a621e-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="a621e-174">**Codebeispiel 2 – `ActionFilters\LogActionFilter.cs`**</span><span class="sxs-lookup"><span data-stu-id="a621e-174">**Listing 2 – `ActionFilters\LogActionFilter.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample2.cs)]

<span data-ttu-id="a621e-175">In der Liste 2 wird mit den Methoden `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`und `OnResultExecuted()` die `Log()`-Methode aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="a621e-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="a621e-176">Der Name der Methode und die aktuellen Routendaten werden an die `Log()` Methode weitergegeben.</span><span class="sxs-lookup"><span data-stu-id="a621e-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="a621e-177">Die `Log()`-Methode schreibt eine Meldung in das Visual Studio-Ausgabefenster (siehe Abbildung 2).</span><span class="sxs-lookup"><span data-stu-id="a621e-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>

<span data-ttu-id="a621e-178">[![schreiben in das Visual Studio-Ausgabefenster](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="a621e-178">[![Writing to the Visual Studio Output window](understanding-action-filters-cs/_static/image5.png)](understanding-action-filters-cs/_static/image4.png)</span></span>

<span data-ttu-id="a621e-179">**Abbildung 02**: Schreiben in das Visual Studio-Ausgabefenster ([Klicken Sie, um das Bild in voller Größe anzuzeigen](understanding-action-filters-cs/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="a621e-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-cs/_static/image6.png))</span></span>

<span data-ttu-id="a621e-180">Der Home-Controller in der Liste 3 veranschaulicht, wie Sie den Protokoll Aktionsfilter auf eine gesamte Controller Klasse anwenden können.</span><span class="sxs-lookup"><span data-stu-id="a621e-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="a621e-181">Wenn eine der vom Home-Controller verfügbar gemachten Aktionen aufgerufen wird – entweder die `Index()`-Methode oder die `About()`-Methode – werden die Phasen der Verarbeitung der Aktion im Ausgabefenster von Visual Studio protokolliert.</span><span class="sxs-lookup"><span data-stu-id="a621e-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="a621e-182">**Codebeispiel 3 – `Controllers\HomeController.cs`**</span><span class="sxs-lookup"><span data-stu-id="a621e-182">**Listing 3 – `Controllers\HomeController.cs`**</span></span>

[!code-csharp[Main](understanding-action-filters-cs/samples/sample3.cs)]

### <a name="summary"></a><span data-ttu-id="a621e-183">Summary</span><span class="sxs-lookup"><span data-stu-id="a621e-183">Summary</span></span>

<span data-ttu-id="a621e-184">In diesem Tutorial haben Sie ASP.NET MVC Action Filters eingeführt.</span><span class="sxs-lookup"><span data-stu-id="a621e-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="a621e-185">Sie haben die vier verschiedenen Arten von Filtern kennengelernt: Autorisierungs Filter, Aktionsfilter, Ergebnis Filter und Ausnahme Filter.</span><span class="sxs-lookup"><span data-stu-id="a621e-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="a621e-186">Außerdem haben Sie mehr über die Basis `ActionFilterAttribute` Klasse erfahren.</span><span class="sxs-lookup"><span data-stu-id="a621e-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="a621e-187">Schließlich haben Sie erfahren, wie Sie einen einfachen Aktionsfilter implementieren.</span><span class="sxs-lookup"><span data-stu-id="a621e-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="a621e-188">Wir haben einen Protokoll Aktionsfilter erstellt, der die Phasen der Verarbeitung einer Controller Aktion im Visual Studio-Ausgabefenster protokolliert.</span><span class="sxs-lookup"><span data-stu-id="a621e-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="a621e-189">[Zurück](asp-net-mvc-routing-overview-cs.md)
> [Weiter](improving-performance-with-output-caching-cs.md)</span><span class="sxs-lookup"><span data-stu-id="a621e-189">[Previous](asp-net-mvc-routing-overview-cs.md)
[Next](improving-performance-with-output-caching-cs.md)</span></span>
