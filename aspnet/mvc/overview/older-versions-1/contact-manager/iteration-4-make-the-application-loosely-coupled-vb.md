---
uid: mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
title: 'Iterations #4 – Festlegen der Anwendung lose gekoppelt (VB) | Microsoft-Dokumentation'
author: microsoft
description: In dieser vierten Iterationen nutzen wir mehrere Software Entwurfsmuster, um die Verwaltung und Änderung der Contact Manager-Anwendung zu vereinfachen. Für ...
ms.author: riande
ms.date: 02/20/2009
ms.assetid: 92c70297-4430-4e4e-919a-9c2333a8d09a
msc.legacyurl: /mvc/overview/older-versions-1/contact-manager/iteration-4-make-the-application-loosely-coupled-vb
msc.type: authoredcontent
ms.openlocfilehash: 422c75406d9c08279d0c2224ee4b6db3a71eb1b3
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 03/06/2020
ms.locfileid: "78470223"
---
# <a name="iteration-4--make-the-application-loosely-coupled-vb"></a><span data-ttu-id="568de-104">Iterations #4 – Festlegen der Anwendung lose gekoppelt (VB)</span><span class="sxs-lookup"><span data-stu-id="568de-104">Iteration #4 – Make the application loosely coupled (VB)</span></span>

<span data-ttu-id="568de-105">von [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="568de-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="568de-106">Code herunterladen</span><span class="sxs-lookup"><span data-stu-id="568de-106">Download Code</span></span>](iteration-4-make-the-application-loosely-coupled-vb/_static/contactmanager_4_vb1.zip)

> <span data-ttu-id="568de-107">In dieser vierten Iterationen nutzen wir mehrere Software Entwurfsmuster, um die Verwaltung und Änderung der Contact Manager-Anwendung zu vereinfachen.</span><span class="sxs-lookup"><span data-stu-id="568de-107">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="568de-108">Beispielsweise können wir unsere Anwendung so umgestalten, dass Sie das Repository-Muster und das Muster für die Abhängigkeitsinjektion verwendet.</span><span class="sxs-lookup"><span data-stu-id="568de-108">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

## <a name="building-a-contact-management-aspnet-mvc-application-vb"></a><span data-ttu-id="568de-109">Aufbauen einer Kontakt Verwaltung ASP.NET MVC-Anwendung (VB)</span><span class="sxs-lookup"><span data-stu-id="568de-109">Building a Contact Management ASP.NET MVC Application (VB)</span></span>

<span data-ttu-id="568de-110">In dieser Reihe von Tutorials erstellen wir von Anfang bis Ende eine gesamte Kontakt Verwaltungs Anwendung.</span><span class="sxs-lookup"><span data-stu-id="568de-110">In this series of tutorials, we build an entire Contact Management application from start to finish.</span></span> <span data-ttu-id="568de-111">Mithilfe der Kontakt-Manager-Anwendung können Sie Kontaktinformationen (Namen, Telefonnummern und e-Mail-Adressen) für eine Liste von Personen speichern.</span><span class="sxs-lookup"><span data-stu-id="568de-111">The Contact Manager application enables you to store contact information - names, phone numbers and email addresses - for a list of people.</span></span>

<span data-ttu-id="568de-112">Die Anwendung wird über mehrere Iterationen erstellt.</span><span class="sxs-lookup"><span data-stu-id="568de-112">We build the application over multiple iterations.</span></span> <span data-ttu-id="568de-113">Bei jeder Iterationen verbessern wir die Anwendung allmählich.</span><span class="sxs-lookup"><span data-stu-id="568de-113">With each iteration, we gradually improve the application.</span></span> <span data-ttu-id="568de-114">Das Ziel dieses mehrfaches Iterations Ansatzes besteht darin, Ihnen zu ermöglichen, den Grund für jede Änderung zu verstehen.</span><span class="sxs-lookup"><span data-stu-id="568de-114">The goal of this multiple iteration approach is to enable you to understand the reason for each change.</span></span>

- <span data-ttu-id="568de-115">Iterations #1: Erstellen Sie die Anwendung.</span><span class="sxs-lookup"><span data-stu-id="568de-115">Iteration #1 - Create the application.</span></span> <span data-ttu-id="568de-116">In der ersten Iterationen erstellen wir den Kontakt-Manager auf einfachste Art und Weise.</span><span class="sxs-lookup"><span data-stu-id="568de-116">In the first iteration, we create the Contact Manager in the simplest way possible.</span></span> <span data-ttu-id="568de-117">Wir fügen Unterstützung für grundlegende Daten Bank Vorgänge hinzu: Create, Read, Update und DELETE (CRUD).</span><span class="sxs-lookup"><span data-stu-id="568de-117">We add support for basic database operations: Create, Read, Update, and Delete (CRUD).</span></span>

- <span data-ttu-id="568de-118">Iterations #2: machen Sie das Aussehen der Anwendung schön.</span><span class="sxs-lookup"><span data-stu-id="568de-118">Iteration #2 - Make the application look nice.</span></span> <span data-ttu-id="568de-119">In dieser Iterationen verbessern wir die Darstellung der Anwendung durch Ändern der standardmäßigen ASP.NET-MVC-Ansichts Master Seite und des Cascading Stylesheets.</span><span class="sxs-lookup"><span data-stu-id="568de-119">In this iteration, we improve the appearance of the application by modifying the default ASP.NET MVC view master page and cascading style sheet.</span></span>

- <span data-ttu-id="568de-120">Iterations #3: Formular Validierung hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="568de-120">Iteration #3 - Add form validation.</span></span> <span data-ttu-id="568de-121">In der dritten Iterationen fügen wir die grundlegende Formular Validierung hinzu.</span><span class="sxs-lookup"><span data-stu-id="568de-121">In the third iteration, we add basic form validation.</span></span> <span data-ttu-id="568de-122">Wir hindern Personen daran, ein Formular zu senden, ohne die erforderlichen Formularfelder abzuschließen.</span><span class="sxs-lookup"><span data-stu-id="568de-122">We prevent people from submitting a form without completing required form fields.</span></span> <span data-ttu-id="568de-123">Wir überprüfen auch die e-Mail-Adressen und Telefonnummern.</span><span class="sxs-lookup"><span data-stu-id="568de-123">We also validate email addresses and phone numbers.</span></span>

- <span data-ttu-id="568de-124">Iterations #4: Legen Sie die Anwendung lose gekoppelt.</span><span class="sxs-lookup"><span data-stu-id="568de-124">Iteration #4 - Make the application loosely coupled.</span></span> <span data-ttu-id="568de-125">In dieser vierten Iterationen nutzen wir mehrere Software Entwurfsmuster, um die Verwaltung und Änderung der Contact Manager-Anwendung zu vereinfachen.</span><span class="sxs-lookup"><span data-stu-id="568de-125">In this fourth iteration, we take advantage of several software design patterns to make it easier to maintain and modify the Contact Manager application.</span></span> <span data-ttu-id="568de-126">Beispielsweise können wir unsere Anwendung so umgestalten, dass Sie das Repository-Muster und das Muster für die Abhängigkeitsinjektion verwendet.</span><span class="sxs-lookup"><span data-stu-id="568de-126">For example, we refactor our application to use the Repository pattern and the Dependency Injection pattern.</span></span>

- <span data-ttu-id="568de-127">Iterations #5: Erstellen von Komponententests.</span><span class="sxs-lookup"><span data-stu-id="568de-127">Iteration #5 - Create unit tests.</span></span> <span data-ttu-id="568de-128">In der fünften Iterationen wird die Wartung und Änderung unserer Anwendung durch Hinzufügen von Komponententests vereinfacht.</span><span class="sxs-lookup"><span data-stu-id="568de-128">In the fifth iteration, we make our application easier to maintain and modify by adding unit tests.</span></span> <span data-ttu-id="568de-129">Wir simulieren unsere Datenmodell Klassen und erstellen Komponententests für unsere Controller und Validierungs Logik.</span><span class="sxs-lookup"><span data-stu-id="568de-129">We mock our data model classes and build unit tests for our controllers and validation logic.</span></span>

- <span data-ttu-id="568de-130">Iterations #6: Verwenden Sie die Test gesteuerte Entwicklung.</span><span class="sxs-lookup"><span data-stu-id="568de-130">Iteration #6 - Use test-driven development.</span></span> <span data-ttu-id="568de-131">In dieser sechsten Iterationen fügen wir der Anwendung neue Funktionen hinzu, indem wir zuerst Komponententests schreiben und Code für die Komponententests schreiben.</span><span class="sxs-lookup"><span data-stu-id="568de-131">In this sixth iteration, we add new functionality to our application by writing unit tests first and writing code against the unit tests.</span></span> <span data-ttu-id="568de-132">In dieser Iterationen fügen wir Kontaktgruppen hinzu.</span><span class="sxs-lookup"><span data-stu-id="568de-132">In this iteration, we add contact groups.</span></span>

- <span data-ttu-id="568de-133">Iterations #7: Hinzufügen von AJAX-Funktionen.</span><span class="sxs-lookup"><span data-stu-id="568de-133">Iteration #7 - Add Ajax functionality.</span></span> <span data-ttu-id="568de-134">In der siebten Iterationen verbessern wir die Reaktionsfähigkeit und Leistung unserer Anwendung durch Hinzufügen von Unterstützung für AJAX.</span><span class="sxs-lookup"><span data-stu-id="568de-134">In the seventh iteration, we improve the responsiveness and performance of our application by adding support for Ajax.</span></span>

## <a name="this-iteration"></a><span data-ttu-id="568de-135">Diese Iterations</span><span class="sxs-lookup"><span data-stu-id="568de-135">This Iteration</span></span>

<span data-ttu-id="568de-136">In dieser vierten Iterations Anwendung der Kontakt-Manager-Anwendung wird die Anwendung umgestalten, um die Anwendung leichter gekoppelt zu gestalten.</span><span class="sxs-lookup"><span data-stu-id="568de-136">In this fourth iteration of the Contact Manager application, we refactor the application to make the application more loosely coupled.</span></span> <span data-ttu-id="568de-137">Wenn eine Anwendung lose gekoppelt ist, können Sie den Code in einem Teil der Anwendung ändern, ohne den Code in anderen Teilen der Anwendung ändern zu müssen.</span><span class="sxs-lookup"><span data-stu-id="568de-137">When an application is loosely coupled, you can modify the code in one part of the application without needing to modify the code in other parts of the application.</span></span> <span data-ttu-id="568de-138">Lose gekoppelte Anwendungen sind stabiler zu ändern.</span><span class="sxs-lookup"><span data-stu-id="568de-138">Loosely coupled applications are more resilient to change.</span></span>

<span data-ttu-id="568de-139">Derzeit sind die Datenzugriffs-und Validierungs Logik, die von der Contact Manager-Anwendung verwendet wird, in den Controller Klassen enthalten.</span><span class="sxs-lookup"><span data-stu-id="568de-139">Currently, all of the data access and validation logic used by the Contact Manager application is contained in the controller classes.</span></span> <span data-ttu-id="568de-140">Das ist eine gute Idee.</span><span class="sxs-lookup"><span data-stu-id="568de-140">This is a bad idea.</span></span> <span data-ttu-id="568de-141">Wenn Sie einen Teil Ihrer Anwendung ändern müssen, riskieren Sie, dass Sie Fehler in einen anderen Teil der Anwendung einführen.</span><span class="sxs-lookup"><span data-stu-id="568de-141">Whenever you need to modify one part of your application, you risk introducing bugs into another part of your application.</span></span> <span data-ttu-id="568de-142">Wenn Sie z. b. die Validierungs Logik ändern, riskieren Sie, dass neue Fehler in den Datenzugriff oder die Controller Logik eingeführt werden.</span><span class="sxs-lookup"><span data-stu-id="568de-142">For example, if you modify your validation logic, you risk introducing new bugs into your data access or controller logic.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="568de-143">(SRP), eine Klasse sollte nie mehr als einen Grund für die Änderung aufweisen.</span><span class="sxs-lookup"><span data-stu-id="568de-143">(SRP), a class should never have more than one reason to change.</span></span> <span data-ttu-id="568de-144">Das Kombinieren von Controller, Validierung und Daten Bank Logik stellt einen massiven Verstoß gegen das Prinzip der einzelnen Verantwortung dar.</span><span class="sxs-lookup"><span data-stu-id="568de-144">Mixing controller, validation, and database logic is a massive violation of the Single Responsibility Principle.</span></span>

<span data-ttu-id="568de-145">Es gibt mehrere Gründe, die Sie möglicherweise benötigen, um Ihre Anwendung zu ändern.</span><span class="sxs-lookup"><span data-stu-id="568de-145">There are several reasons that you might need to modify your application.</span></span> <span data-ttu-id="568de-146">Möglicherweise müssen Sie der Anwendung ein neues Feature hinzufügen. möglicherweise müssen Sie einen Fehler in Ihrer Anwendung beheben, oder Sie müssen ggf. ändern, wie ein Feature der Anwendung implementiert wird.</span><span class="sxs-lookup"><span data-stu-id="568de-146">You might need to add a new feature to your application, you might need to fix a bug in your application, or you might need to modify how a feature of your application is implemented.</span></span> <span data-ttu-id="568de-147">Anwendungen sind selten statisch.</span><span class="sxs-lookup"><span data-stu-id="568de-147">Applications are rarely static.</span></span> <span data-ttu-id="568de-148">Sie werden tendenziell vergrößert und im Laufe der Zeit mutiert.</span><span class="sxs-lookup"><span data-stu-id="568de-148">They tend to grow and mutate over time.</span></span>

<span data-ttu-id="568de-149">Stellen Sie sich beispielsweise vor, dass Sie sich entscheiden, wie Sie Ihre Datenzugriffs Ebene implementieren.</span><span class="sxs-lookup"><span data-stu-id="568de-149">Imagine, for example, that you decide to change how you implement your data access layer.</span></span> <span data-ttu-id="568de-150">Derzeit verwendet die Contact Manager-Anwendung den Microsoft-Entity Framework, um auf die Datenbank zuzugreifen.</span><span class="sxs-lookup"><span data-stu-id="568de-150">Right now, the Contact Manager application uses the Microsoft Entity Framework to access the database.</span></span> <span data-ttu-id="568de-151">Sie können jedoch eine Migration zu einer neuen oder alternativen Datenzugriffs Technologie durchsetzen, z. b. ADO.NET Data Services oder NHibernate.</span><span class="sxs-lookup"><span data-stu-id="568de-151">However, you might decide to migrate to a new or alternative data access technology such as ADO.NET Data Services or NHibernate.</span></span> <span data-ttu-id="568de-152">Da der Datenzugriffs Code jedoch nicht vom Validierungs-und Controller Code isoliert ist, gibt es keine Möglichkeit, den Datenzugriffs Code in der Anwendung zu ändern, ohne anderen Code zu ändern, der nicht direkt mit dem Datenzugriff verknüpft ist.</span><span class="sxs-lookup"><span data-stu-id="568de-152">However, because the data access code is not isolated from the validation and controller code, there is no way to modify the data access code in your application without modifying other code that is not directly related to data access.</span></span>

<span data-ttu-id="568de-153">Wenn eine Anwendung lose gekoppelt ist, können Sie auf der anderen Seite Änderungen an einem Teil der Anwendung vornehmen, ohne andere Teile einer Anwendung zu berühren.</span><span class="sxs-lookup"><span data-stu-id="568de-153">When an application is loosely coupled, on the other hand, you can make changes to one part of an application without touching other parts of an application.</span></span> <span data-ttu-id="568de-154">Beispielsweise können Sie Datenzugriffs Technologien wechseln, ohne die Validierungs-oder Controller Logik zu ändern.</span><span class="sxs-lookup"><span data-stu-id="568de-154">For example, you can switch data access technologies without modifying your validation or controller logic.</span></span>

<span data-ttu-id="568de-155">In dieser Iterationen nutzen wir mehrere Software Entwurfsmuster, mit denen wir unsere Kontakt-Manager-Anwendung in eine lose gekoppelte Anwendung umgestalten können.</span><span class="sxs-lookup"><span data-stu-id="568de-155">In this iteration, we take advantage of several software design patterns that enable us to refactor our Contact Manager application into a more loosely coupled application.</span></span> <span data-ttu-id="568de-156">Wenn Sie den Vorgang abgeschlossen haben, hat der Kontakt-Manager keine weiteren Aktionen ausgeführt, die er zuvor nicht ausgeführt hat.</span><span class="sxs-lookup"><span data-stu-id="568de-156">When we are done, the Contact Manager won t do anything that it didn't do before.</span></span> <span data-ttu-id="568de-157">Die Anwendung kann jedoch in Zukunft einfacher geändert werden.</span><span class="sxs-lookup"><span data-stu-id="568de-157">However, we'll be able to change the application more easily in the future.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="568de-158">Refactoring ist der Prozess, bei dem eine Anwendung so umgeschrieben wird, dass Sie keinerlei vorhandene Funktionalität verliert.</span><span class="sxs-lookup"><span data-stu-id="568de-158">Refactoring is the process of rewriting an application in such a way that it does not lose any existing functionality.</span></span>

## <a name="using-the-repository-software-design-pattern"></a><span data-ttu-id="568de-159">Verwenden des Repository-Software Entwurfs Musters</span><span class="sxs-lookup"><span data-stu-id="568de-159">Using the Repository Software Design Pattern</span></span>

<span data-ttu-id="568de-160">Unsere erste Änderung besteht darin, ein Software Entwurfsmuster zu nutzen, das als Repository-Muster bezeichnet wird.</span><span class="sxs-lookup"><span data-stu-id="568de-160">Our first change is to take advantage of a software design pattern called the Repository pattern.</span></span> <span data-ttu-id="568de-161">Wir verwenden das Repository-Muster, um den Datenzugriffs Code von der restlichen Anwendung zu isolieren.</span><span class="sxs-lookup"><span data-stu-id="568de-161">We'll use the Repository pattern to isolate our data access code from the rest of our application.</span></span>

<span data-ttu-id="568de-162">Zum Implementieren des Repository-Musters müssen wir die folgenden beiden Schritte ausführen:</span><span class="sxs-lookup"><span data-stu-id="568de-162">Implementing the Repository pattern requires us to complete the following two steps:</span></span>

1. <span data-ttu-id="568de-163">Erstellen einer Schnittstelle</span><span class="sxs-lookup"><span data-stu-id="568de-163">Create an interface</span></span>
2. <span data-ttu-id="568de-164">Erstellen einer konkreten Klasse, die die-Schnittstelle implementiert</span><span class="sxs-lookup"><span data-stu-id="568de-164">Create a concrete class that implements the interface</span></span>

<span data-ttu-id="568de-165">Zuerst müssen wir eine Schnittstelle erstellen, die alle Datenzugriffs Methoden beschreibt, die wir ausführen müssen.</span><span class="sxs-lookup"><span data-stu-id="568de-165">First, we need to create an interface that describes all of the data access methods that we need to perform.</span></span> <span data-ttu-id="568de-166">Die icontactmanagerrepository-Schnittstelle ist in der Liste 1 enthalten.</span><span class="sxs-lookup"><span data-stu-id="568de-166">The IContactManagerRepository interface is contained in Listing 1.</span></span> <span data-ttu-id="568de-167">Diese Schnittstelle beschreibt fünf Methoden: "kreatecontact ()", "DeleteContact ()", "EditContact ()", "GetContact" und "listcontacts ()".</span><span class="sxs-lookup"><span data-stu-id="568de-167">This interface describes five methods: CreateContact(), DeleteContact(), EditContact(), GetContact, and ListContacts().</span></span>

<span data-ttu-id="568de-168">**Codebeispiel 1: models\icontactmanagerrepositoriy.vb**</span><span class="sxs-lookup"><span data-stu-id="568de-168">**Listing 1 - Models\IContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample1.vb)]

<span data-ttu-id="568de-169">Als nächstes müssen wir eine konkrete Klasse erstellen, die die icontactmanagerrepository-Schnittstelle implementiert.</span><span class="sxs-lookup"><span data-stu-id="568de-169">Next, we need to create a concrete class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="568de-170">Da wir den Microsoft-Entity Framework für den Zugriff auf die Datenbank verwenden, erstellen wir eine neue Klasse mit dem Namen entitycontactmanagerrepository.</span><span class="sxs-lookup"><span data-stu-id="568de-170">Because we are using the Microsoft Entity Framework to access the database, we'll create a new class named EntityContactManagerRepository.</span></span> <span data-ttu-id="568de-171">Diese Klasse ist in der Liste 2 enthalten.</span><span class="sxs-lookup"><span data-stu-id="568de-171">This class is contained in Listing 2.</span></span>

<span data-ttu-id="568de-172">**Codebeispiel 2: models\entitycontactmanagerrepositorien.vb**</span><span class="sxs-lookup"><span data-stu-id="568de-172">**Listing 2 - Models\EntityContactManagerRepository.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample2.vb)]

<span data-ttu-id="568de-173">Beachten Sie, dass die entitycontactmanagerrepository-Klasse die icontactmanagerrepository-Schnittstelle implementiert.</span><span class="sxs-lookup"><span data-stu-id="568de-173">Notice that the EntityContactManagerRepository class implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="568de-174">Die-Klasse implementiert alle fünf Methoden, die von dieser Schnittstelle beschrieben werden.</span><span class="sxs-lookup"><span data-stu-id="568de-174">The class implements all five of the methods described by that interface.</span></span>

<span data-ttu-id="568de-175">Vielleicht Fragen Sie sich, warum wir uns mit einer Schnittstelle beschäftigen müssen.</span><span class="sxs-lookup"><span data-stu-id="568de-175">You might wonder why we need to bother with an interface.</span></span> <span data-ttu-id="568de-176">Warum müssen wir sowohl eine Schnittstelle als auch eine Klasse erstellen, die Sie implementiert?</span><span class="sxs-lookup"><span data-stu-id="568de-176">Why do we need to create both an interface and a class that implements it?</span></span>

<span data-ttu-id="568de-177">Mit einer Ausnahme interagiert der Rest unserer Anwendung mit der-Schnittstelle und nicht mit der konkreten-Klasse.</span><span class="sxs-lookup"><span data-stu-id="568de-177">With one exception, the remainder of our application will interact with the interface and not the concrete class.</span></span> <span data-ttu-id="568de-178">Anstatt die Methoden aufzurufen, die von der entitycontactmanagerrepository-Klasse verfügbar gemacht werden, werden die Methoden aufgerufen, die von der icontactmanagerrepository-Schnittstelle verfügbar gemacht werden.</span><span class="sxs-lookup"><span data-stu-id="568de-178">Instead of calling the methods exposed by the EntityContactManagerRepository class, we'll call the methods exposed by the IContactManagerRepository interface.</span></span>

<span data-ttu-id="568de-179">Auf diese Weise können wir die Schnittstelle mit einer neuen Klasse implementieren, ohne den Rest der Anwendung ändern zu müssen.</span><span class="sxs-lookup"><span data-stu-id="568de-179">That way, we can implement the interface with a new class without needing to modify the remainder of our application.</span></span> <span data-ttu-id="568de-180">Beispielsweise möchten wir zu einem späteren Zeitpunkt möglicherweise eine dataservicescontactmanagerrepository-Klasse implementieren, die die icontactmanagerrepository-Schnittstelle implementiert.</span><span class="sxs-lookup"><span data-stu-id="568de-180">For example, at some future date, we might want to implement an DataServicesContactManagerRepository class that implements the IContactManagerRepository interface.</span></span> <span data-ttu-id="568de-181">Die dataservicescontactmanagerrepository-Klasse kann ADO.NET-Data Services verwenden, um auf eine Datenbank anstatt auf die Microsoft-Entity Framework zuzugreifen.</span><span class="sxs-lookup"><span data-stu-id="568de-181">The DataServicesContactManagerRepository class might use ADO.NET Data Services to access a database instead of the Microsoft Entity Framework.</span></span>

<span data-ttu-id="568de-182">Wenn der Anwendungscode für die icontactmanagerrepository-Schnittstelle und nicht für die konkrete entitycontactmanagerrepository-Klasse programmiert wird, können wir konkrete Klassen wechseln, ohne den restlichen Code zu ändern.</span><span class="sxs-lookup"><span data-stu-id="568de-182">If our application code is programmed against the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class then we can switch concrete classes without modifying any of the rest of our code.</span></span> <span data-ttu-id="568de-183">Beispielsweise können wir von der entitycontactmanagerrepository-Klasse zur dataservicescontactmanagerrepository-Klasse wechseln, ohne den Datenzugriff oder die Validierungs Logik zu ändern.</span><span class="sxs-lookup"><span data-stu-id="568de-183">For example, we can switch from the EntityContactManagerRepository class to the DataServicesContactManagerRepository class without modifying our data access or validation logic.</span></span>

<span data-ttu-id="568de-184">Die Programmierung für Schnittstellen (Abstraktionen) anstelle von konkreten klassenmacht unsere Anwendung stabiler, um sich zu ändern.</span><span class="sxs-lookup"><span data-stu-id="568de-184">Programming against interfaces (abstractions) instead of concrete classes makes our application more resilient to change.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="568de-185">Sie können schnell eine Schnittstelle aus einer konkreten Klasse in Visual Studio erstellen, indem Sie die Menüoption umgestalten, Schnittstelle extrahieren auswählen.</span><span class="sxs-lookup"><span data-stu-id="568de-185">You can quickly create an interface from a concrete class within Visual Studio by selecting the menu option Refactor, Extract Interface.</span></span> <span data-ttu-id="568de-186">Beispielsweise können Sie zuerst die entitycontactmanagerrepository-Klasse erstellen und dann mit Extract Interface die icontactmanagerrepository-Schnittstelle automatisch generieren.</span><span class="sxs-lookup"><span data-stu-id="568de-186">For example, you can create the EntityContactManagerRepository class first and then use Extract Interface to generate the IContactManagerRepository interface automatically.</span></span>

## <a name="using-the-dependency-injection-software-design-pattern"></a><span data-ttu-id="568de-187">Verwenden des Software Entwurfs Musters für die Abhängigkeitsinjektion</span><span class="sxs-lookup"><span data-stu-id="568de-187">Using the Dependency Injection Software Design Pattern</span></span>

<span data-ttu-id="568de-188">Nachdem wir den Datenzugriffs Code zu einer separaten Repository-Klasse migriert haben, müssen wir den Kontakt Controller ändern, um diese Klasse zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="568de-188">Now that we have migrated our data access code to a separate Repository class, we need to modify our Contact controller to use this class.</span></span> <span data-ttu-id="568de-189">Wir nutzen ein Software Entwurfsmuster namens "Abhängigkeitsinjektion", um die Repository-Klasse in unserem Controller zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="568de-189">We will take advantage of a software design pattern called Dependency Injection to use the Repository class in our controller.</span></span>

<span data-ttu-id="568de-190">Der geänderte Kontakt Controller ist in der Liste 3 enthalten.</span><span class="sxs-lookup"><span data-stu-id="568de-190">The modified Contact controller is contained in Listing 3.</span></span>

<span data-ttu-id="568de-191">**Codebeispiel 3-controllers\contactcontroller.vb**</span><span class="sxs-lookup"><span data-stu-id="568de-191">**Listing 3 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample3.vb)]

<span data-ttu-id="568de-192">Beachten Sie, dass der Contact Controller in der Liste 3 über zwei Konstruktoren verfügt.</span><span class="sxs-lookup"><span data-stu-id="568de-192">Notice that the Contact controller in Listing 3 has two constructors.</span></span> <span data-ttu-id="568de-193">Der erste Konstruktor übergibt eine konkrete Instanz der icontactmanagerrepository-Schnittstelle an den zweiten Konstruktor.</span><span class="sxs-lookup"><span data-stu-id="568de-193">The first constructor passes a concrete instance of the IContactManagerRepository interface to the second constructor.</span></span> <span data-ttu-id="568de-194">Die Contact Controller-Klasse verwendet die *konstruktorabhängigkeits Injektion*.</span><span class="sxs-lookup"><span data-stu-id="568de-194">The Contact controller class uses *Constructor Dependency Injection*.</span></span>

<span data-ttu-id="568de-195">Die einzige Stelle, an der die entitycontactmanagerrepository-Klasse verwendet wird, ist der erste Konstruktor.</span><span class="sxs-lookup"><span data-stu-id="568de-195">The one and only place that the EntityContactManagerRepository class is used is in the first constructor.</span></span> <span data-ttu-id="568de-196">Der Rest der Klasse verwendet die icontactmanagerrepository-Schnittstelle anstelle der konkreten entitycontactmanagerrepository-Klasse.</span><span class="sxs-lookup"><span data-stu-id="568de-196">The remainder of the class uses the IContactManagerRepository interface instead of the concrete EntityContactManagerRepository class.</span></span>

<span data-ttu-id="568de-197">Dadurch können Implementierungen der icontactmanagerrepository-Klasse in Zukunft einfach gewechselt werden.</span><span class="sxs-lookup"><span data-stu-id="568de-197">This makes it easy to switch implementations of the IContactManagerRepository class in the future.</span></span> <span data-ttu-id="568de-198">Wenn Sie anstelle der entitycontactmanagerrepository-Klasse die dataservicescontactrepository-Klasse verwenden möchten, ändern Sie einfach den ersten Konstruktor.</span><span class="sxs-lookup"><span data-stu-id="568de-198">If you want to use the DataServicesContactRepository class instead of the EntityContactManagerRepository class, just modify the first constructor.</span></span>

<span data-ttu-id="568de-199">Mit der konstruktorabhängigkeits Injektion wird die Contact Controller-Klasse ebenfalls sehr testfähig.</span><span class="sxs-lookup"><span data-stu-id="568de-199">Constructor Dependency injection also makes the Contact controller class very testable.</span></span> <span data-ttu-id="568de-200">In den Komponententests können Sie den Kontakt Controller instanziieren, indem Sie eine Mock-Implementierung der icontactmanagerrepository-Klasse übergeben.</span><span class="sxs-lookup"><span data-stu-id="568de-200">In your unit tests, you can instantiate the Contact controller by passing a mock implementation of the IContactManagerRepository class.</span></span> <span data-ttu-id="568de-201">Diese Funktion der Abhängigkeitsinjektion ist für uns in der nächsten Iterationen sehr wichtig, wenn Komponententests für die Contact Manager-Anwendung erstellt werden.</span><span class="sxs-lookup"><span data-stu-id="568de-201">This feature of Dependency Injection will be very important to us in the next iteration when we build unit tests for the Contact Manager application.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="568de-202">Wenn Sie die Contact Controller-Klasse von einer bestimmten Implementierung der icontactmanagerrepository-Schnittstelle vollständig entkoppeln möchten, können Sie ein Framework nutzen, das die Abhängigkeitsinjektion unterstützt, wie z. b. StructureMap oder Microsoft Entity Framework (MEF).</span><span class="sxs-lookup"><span data-stu-id="568de-202">If you want to completely decouple the Contact controller class from a particular implementation of the IContactManagerRepository interface then you can take advantage of a framework that supports Dependency Injection such as StructureMap or the Microsoft Entity Framework (MEF).</span></span> <span data-ttu-id="568de-203">Wenn Sie ein Framework für die Abhängigkeitsinjektion nutzen, müssen Sie in Ihrem Code nie auf eine konkrete Klasse verweisen.</span><span class="sxs-lookup"><span data-stu-id="568de-203">By taking advantage of a Dependency Injection framework, you never need to refer to a concrete class in your code.</span></span>

## <a name="creating-a-service-layer"></a><span data-ttu-id="568de-204">Erstellen einer Dienst Ebene</span><span class="sxs-lookup"><span data-stu-id="568de-204">Creating a Service Layer</span></span>

<span data-ttu-id="568de-205">Sie haben vielleicht bemerkt, dass unsere Validierungs Logik weiterhin mit unserer Controller Logik in der geänderten Controller Klasse in der Liste 3 vermischt wird.</span><span class="sxs-lookup"><span data-stu-id="568de-205">You might have noticed that our validation logic is still mixed up with our controller logic in the modified controller class in Listing 3.</span></span> <span data-ttu-id="568de-206">Aus demselben Grund ist es eine gute Idee, unsere Datenzugriffs Logik zu isolieren. es ist eine gute Idee, unsere Validierungs Logik zu isolieren.</span><span class="sxs-lookup"><span data-stu-id="568de-206">For the same reason that it is a good idea to isolate our data access logic, it is a good idea to isolate our validation logic.</span></span>

<span data-ttu-id="568de-207">Um dieses Problem zu beheben, können wir eine separate [Dienst Schicht](http://martinfowler.com/eaaCatalog/serviceLayer.html)erstellen.</span><span class="sxs-lookup"><span data-stu-id="568de-207">To fix this problem, we can create a separate [service layer](http://martinfowler.com/eaaCatalog/serviceLayer.html).</span></span> <span data-ttu-id="568de-208">Die Dienst Ebene ist eine separate Ebene, die zwischen den Controller-und Repository-Klassen eingefügt werden kann.</span><span class="sxs-lookup"><span data-stu-id="568de-208">The service layer is a separate layer that we can insert between our controller and repository classes.</span></span> <span data-ttu-id="568de-209">Die Dienst Ebene enthält unsere Geschäftslogik, einschließlich der gesamten Validierungs Logik.</span><span class="sxs-lookup"><span data-stu-id="568de-209">The service layer contains our business logic including all of our validation logic.</span></span>

<span data-ttu-id="568de-210">Contactmanagerservice ist in der Liste 4 enthalten.</span><span class="sxs-lookup"><span data-stu-id="568de-210">The ContactManagerService is contained in Listing 4.</span></span> <span data-ttu-id="568de-211">Sie enthält die Validierungs Logik der Contact Controller-Klasse.</span><span class="sxs-lookup"><span data-stu-id="568de-211">It contains the validation logic from the Contact controller class.</span></span>

<span data-ttu-id="568de-212">**Codebeispiel 4: models\contactmanagerservice.vb**</span><span class="sxs-lookup"><span data-stu-id="568de-212">**Listing 4 - Models\ContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample4.vb)]

<span data-ttu-id="568de-213">Beachten Sie, dass der Konstruktor für den contactmanagerservice ein validationdictionary erfordert.</span><span class="sxs-lookup"><span data-stu-id="568de-213">Notice that the constructor for the ContactManagerService requires a ValidationDictionary.</span></span> <span data-ttu-id="568de-214">Die Dienst Ebene kommuniziert über dieses validationdictionary mit der Controller Schicht.</span><span class="sxs-lookup"><span data-stu-id="568de-214">The service layer communicates with the controller layer through this ValidationDictionary.</span></span> <span data-ttu-id="568de-215">Im folgenden Abschnitt wird das validationdictionary ausführlich erläutert, wenn wir das Decorator-Muster erörtern.</span><span class="sxs-lookup"><span data-stu-id="568de-215">We discuss the ValidationDictionary in detail in the following section when we discuss the Decorator pattern.</span></span>

<span data-ttu-id="568de-216">Beachten Sie, dass der contactmanagerservice die Schnittstelle icontactmanagerservice implementiert.</span><span class="sxs-lookup"><span data-stu-id="568de-216">Notice, furthermore, that the ContactManagerService implements the IContactManagerService interface.</span></span> <span data-ttu-id="568de-217">Sie sollten sich immer darauf konzentrieren, anstelle von konkreten Klassen für Schnittstellen zu programmieren.</span><span class="sxs-lookup"><span data-stu-id="568de-217">You should always strive to program against interfaces instead of concrete classes.</span></span> <span data-ttu-id="568de-218">Andere Klassen in der Contact Manager-Anwendung interagieren nicht direkt mit der contactmanagerservice-Klasse.</span><span class="sxs-lookup"><span data-stu-id="568de-218">Other classes in the Contact Manager application do not interact with the ContactManagerService class directly.</span></span> <span data-ttu-id="568de-219">Stattdessen wird der Rest der Kontakt-Manager-Anwendung mit der icontactmanagerservice-Schnittstelle programmiert, mit einer Ausnahme.</span><span class="sxs-lookup"><span data-stu-id="568de-219">Instead, with one exception, the remainder of the Contact Manager application is programmed against the IContactManagerService interface.</span></span>

<span data-ttu-id="568de-220">Die icontactmanagerservice-Schnittstelle ist in der Liste 5 enthalten.</span><span class="sxs-lookup"><span data-stu-id="568de-220">The IContactManagerService interface is contained in Listing 5.</span></span>

<span data-ttu-id="568de-221">**Auflisten 5: models\icontactmanagerservice.vb**</span><span class="sxs-lookup"><span data-stu-id="568de-221">**Listing 5 - Models\IContactManagerService.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample5.vb)]

<span data-ttu-id="568de-222">Die geänderte Contact Controller-Klasse ist in der Liste 6 enthalten.</span><span class="sxs-lookup"><span data-stu-id="568de-222">The modified Contact controller class is contained in Listing 6.</span></span> <span data-ttu-id="568de-223">Beachten Sie, dass der Contact Controller nicht mehr mit dem ContactManager-Repository interagiert.</span><span class="sxs-lookup"><span data-stu-id="568de-223">Notice that the Contact controller no longer interacts with the ContactManager repository.</span></span> <span data-ttu-id="568de-224">Stattdessen interagiert der Kontakt Controller mit dem ContactManager-Dienst.</span><span class="sxs-lookup"><span data-stu-id="568de-224">Instead, the Contact controller interacts with the ContactManager service.</span></span> <span data-ttu-id="568de-225">Jede Ebene wird so weit wie möglich von anderen Ebenen isoliert.</span><span class="sxs-lookup"><span data-stu-id="568de-225">Each layer is isolated as much as possible from other layers.</span></span>

<span data-ttu-id="568de-226">**Auflisten 6-controllers\contactcontroller.vb**</span><span class="sxs-lookup"><span data-stu-id="568de-226">**Listing 6 - Controllers\ContactController.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample6.vb)]

<span data-ttu-id="568de-227">Unsere Anwendung wird nicht mehr mit dem Prinzip der einzelnen Zuständigkeit (SRP) ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="568de-227">Our application no longer runs afoul of the Single Responsibility Principle (SRP).</span></span> <span data-ttu-id="568de-228">Der Kontakt-Controller in der Liste 6 wurde für alle Verantwortlichkeiten außer der Steuerung des Anwendungs Ausführungs Flusses entfernt.</span><span class="sxs-lookup"><span data-stu-id="568de-228">The Contact controller in Listing 6 has been stripped of every responsibility other than controlling the flow of application execution.</span></span> <span data-ttu-id="568de-229">Die gesamte Validierungs Logik wurde aus dem Contact Controller entfernt und in die Dienst Schicht übermittelt.</span><span class="sxs-lookup"><span data-stu-id="568de-229">All the validation logic has been removed from the Contact controller and pushed into the service layer.</span></span> <span data-ttu-id="568de-230">Die gesamte Daten Bank Logik wurde in die Repository-Schicht übermittelt.</span><span class="sxs-lookup"><span data-stu-id="568de-230">All of the database logic has been pushed into the repository layer.</span></span>

## <a name="using-the-decorator-pattern"></a><span data-ttu-id="568de-231">Verwenden des Decorator-Musters</span><span class="sxs-lookup"><span data-stu-id="568de-231">Using the Decorator Pattern</span></span>

<span data-ttu-id="568de-232">Wir möchten unsere Service Schicht vollständig von unserer Controller Schicht entkoppeln.</span><span class="sxs-lookup"><span data-stu-id="568de-232">We want to be able to completely decouple our service layer from our controller layer.</span></span> <span data-ttu-id="568de-233">Im Prinzip sollten wir unsere Dienst Schicht in einer separaten Assembly von unserer Controller Schicht kompilieren können, ohne einen Verweis auf unsere MVC-Anwendung hinzufügen zu müssen.</span><span class="sxs-lookup"><span data-stu-id="568de-233">In principle, we should be able to compile our service layer in a separate assembly from our controller layer without needing to add a reference to our MVC application.</span></span>

<span data-ttu-id="568de-234">Unsere Dienst Schicht muss jedoch in der Lage sein, Validierungs Fehlermeldungen zurück an die Controller Schicht zu übergeben.</span><span class="sxs-lookup"><span data-stu-id="568de-234">However, our service layer needs to be able to pass validation error messages back to the controller layer.</span></span> <span data-ttu-id="568de-235">Wie können wir es der Dienst Ebene ermöglichen, Validierungs Fehlermeldungen zu übermitteln, ohne den Controller und die Dienst Schicht zu koppeln?</span><span class="sxs-lookup"><span data-stu-id="568de-235">How can we enable the service layer to communicate validation error messages without coupling the controller and service layer?</span></span> <span data-ttu-id="568de-236">Wir können ein Software Entwurfsmuster mit dem Namen [Decorator-Muster](http://en.wikipedia.org/wiki/Decorator_pattern)nutzen.</span><span class="sxs-lookup"><span data-stu-id="568de-236">We can take advantage of a software design pattern named the [Decorator pattern](http://en.wikipedia.org/wiki/Decorator_pattern).</span></span>

<span data-ttu-id="568de-237">Ein Controller verwendet einen modelstatedictionary namens modelstate, um Validierungs Fehler darzustellen.</span><span class="sxs-lookup"><span data-stu-id="568de-237">A controller uses a ModelStateDictionary named ModelState to represent validation errors.</span></span> <span data-ttu-id="568de-238">Daher ist es möglicherweise verlockend, modelstate von der Controller Schicht an die Dienst Schicht zu übergeben.</span><span class="sxs-lookup"><span data-stu-id="568de-238">Therefore, you might be tempted to pass ModelState from the controller layer to the service layer.</span></span> <span data-ttu-id="568de-239">Die Verwendung von modelstate in der Dienst Ebene würde jedoch dazu führen, dass die Dienst Ebene von einer Funktion des ASP.NET-MVC-Frameworks abhängig ist.</span><span class="sxs-lookup"><span data-stu-id="568de-239">However, using ModelState in the service layer would make your service layer dependent on a feature of the ASP.NET MVC framework.</span></span> <span data-ttu-id="568de-240">Dies wäre schlecht, weil Sie einen Tag lang die Dienst Ebene mit einer WPF-Anwendung anstelle einer ASP.NET MVC-Anwendung verwenden möchten.</span><span class="sxs-lookup"><span data-stu-id="568de-240">This would be bad because, someday, you might want to use the service layer with a WPF application instead of an ASP.NET MVC application.</span></span> <span data-ttu-id="568de-241">In diesem Fall möchten Sie nicht auf das ASP.NET MVC-Framework verweisen, um die modelstatuedictionary-Klasse zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="568de-241">In that case, you wouldn't want to reference the ASP.NET MVC framework to use the ModelStateDictionary class.</span></span>

<span data-ttu-id="568de-242">Das Decorator-Muster ermöglicht es Ihnen, eine vorhandene Klasse in einer neuen Klasse zu umschließen, um eine Schnittstelle zu implementieren.</span><span class="sxs-lookup"><span data-stu-id="568de-242">The Decorator pattern enables you to wrap an existing class in a new class in order to implement an interface.</span></span> <span data-ttu-id="568de-243">Unser Contact Manager-Projekt enthält die modelstatewrapper-Klasse, die in der Liste 7 enthalten ist.</span><span class="sxs-lookup"><span data-stu-id="568de-243">Our Contact Manager project includes the ModelStateWrapper class contained in Listing 7.</span></span> <span data-ttu-id="568de-244">Die modelstatewrapper-Klasse implementiert die-Schnittstelle in der Auflistung 8.</span><span class="sxs-lookup"><span data-stu-id="568de-244">The ModelStateWrapper class implements the interface in Listing 8.</span></span>

<span data-ttu-id="568de-245">**Codebeispiel 7: models\validation\modelstatewrapper.vb**</span><span class="sxs-lookup"><span data-stu-id="568de-245">**Listing 7 - Models\Validation\ModelStateWrapper.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample7.vb)]

<span data-ttu-id="568de-246">**Auflisten von 8-models\validation\ivalidationditionary.vb**</span><span class="sxs-lookup"><span data-stu-id="568de-246">**Listing 8 - Models\Validation\IValidationDictionary.vb**</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample8.vb)]

<span data-ttu-id="568de-247">Wenn Sie sich die Liste 5 genauer ansehen, sehen Sie, dass die Dienst Ebene "ContactManager" die ivalidationdictionary-Schnittstelle exklusiv verwendet.</span><span class="sxs-lookup"><span data-stu-id="568de-247">If you take a close look at Listing 5 then you'll see that the ContactManager service layer uses the IValidationDictionary interface exclusively.</span></span> <span data-ttu-id="568de-248">Der ContactManager-Dienst ist nicht von der modelstatuedictionary-Klasse abhängig.</span><span class="sxs-lookup"><span data-stu-id="568de-248">The ContactManager service is not dependent on the ModelStateDictionary class.</span></span> <span data-ttu-id="568de-249">Wenn der Contact Controller den ContactManager-Dienst erstellt, bindet der Controller seinen modelstate wie folgt ein:</span><span class="sxs-lookup"><span data-stu-id="568de-249">When the Contact controller creates the ContactManager service, the controller wraps its ModelState like this:</span></span>

[!code-vb[Main](iteration-4-make-the-application-loosely-coupled-vb/samples/sample9.vb)]

## <a name="summary"></a><span data-ttu-id="568de-250">Zusammenfassung</span><span class="sxs-lookup"><span data-stu-id="568de-250">Summary</span></span>

<span data-ttu-id="568de-251">In dieser Iterations Funktion haben wir der Contact Manager-Anwendung keine neuen Funktionen hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="568de-251">In this iteration, we did not add any new functionality to the Contact Manager application.</span></span> <span data-ttu-id="568de-252">Das Ziel dieser Iterationen bestand darin, die Kontakt-Manager-Anwendung so zu gestalten, dass Sie einfacher zu verwalten und zu ändern ist.</span><span class="sxs-lookup"><span data-stu-id="568de-252">The goal of this iteration was to refactor the Contact Manager application so that is easier to maintain and modify.</span></span>

<span data-ttu-id="568de-253">Zuerst haben wir das Repository-Software Entwurfsmuster implementiert.</span><span class="sxs-lookup"><span data-stu-id="568de-253">First, we implemented the Repository software design pattern.</span></span> <span data-ttu-id="568de-254">Wir haben den gesamten Datenzugriffs Code zu einer separaten ContactManager-Repository-Klasse migriert.</span><span class="sxs-lookup"><span data-stu-id="568de-254">We migrated all of the data access code to a separate ContactManager repository class.</span></span>

<span data-ttu-id="568de-255">Wir haben auch unsere Validierungs Logik von unserer Controller Logik isoliert.</span><span class="sxs-lookup"><span data-stu-id="568de-255">We also isolated our validation logic from our controller logic.</span></span> <span data-ttu-id="568de-256">Wir haben eine separate Dienst Ebene erstellt, die den gesamten Validierungscode enthält.</span><span class="sxs-lookup"><span data-stu-id="568de-256">We created a separate service layer that contains all of our validation code.</span></span> <span data-ttu-id="568de-257">Die Controller Schicht interagiert mit der Dienst Ebene, und die Dienst Schicht interagiert mit der Repository-Ebene.</span><span class="sxs-lookup"><span data-stu-id="568de-257">The controller layer interacts with the service layer, and the service layer interacts with the repository layer.</span></span>

<span data-ttu-id="568de-258">Als wir die Dienst Ebene erstellt haben, nutzten wir das Decorator-Muster, um modelstate von unserer Dienst Schicht zu isolieren.</span><span class="sxs-lookup"><span data-stu-id="568de-258">When we created the service layer, we took advantage of the Decorator pattern to isolate ModelState from our service layer.</span></span> <span data-ttu-id="568de-259">In unserer Dienst Schicht haben wir anstelle von modelstate die ivalidationdictionary-Schnittstelle programmiert.</span><span class="sxs-lookup"><span data-stu-id="568de-259">In our service layer, we programmed against the IValidationDictionary interface instead of ModelState.</span></span>

<span data-ttu-id="568de-260">Schließlich haben wir ein Software Entwurfsmuster mit dem Namen "Abhängigkeitsinjektion" genutzt.</span><span class="sxs-lookup"><span data-stu-id="568de-260">Finally, we took advantage of a software design pattern named the Dependency Injection pattern.</span></span> <span data-ttu-id="568de-261">Mit diesem Muster können wir anstelle von konkreten Klassen für Schnittstellen (Abstraktionen) programmieren.</span><span class="sxs-lookup"><span data-stu-id="568de-261">This pattern enables us to program against interfaces (abstractions) instead of concrete classes.</span></span> <span data-ttu-id="568de-262">Das Implementieren des Entwurfs Musters für die Abhängigkeitsinjektion sorgt auch für einen testbaren Code.</span><span class="sxs-lookup"><span data-stu-id="568de-262">Implementing the Dependency Injection design pattern also makes our code more testable.</span></span> <span data-ttu-id="568de-263">In der nächsten Iterationen fügen wir dem Projektkomponenten Tests hinzu.</span><span class="sxs-lookup"><span data-stu-id="568de-263">In the next iteration, we add unit tests to our project.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="568de-264">[Zurück](iteration-3-add-form-validation-vb.md)
> [Weiter](iteration-5-create-unit-tests-vb.md)</span><span class="sxs-lookup"><span data-stu-id="568de-264">[Previous](iteration-3-add-form-validation-vb.md)
[Next](iteration-5-create-unit-tests-vb.md)</span></span>
